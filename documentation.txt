================================================================================
PLUS33 ERP — SYSTEM DOCUMENTATION
================================================================================

This document contains the official technical and functional documentation for
the PLUS33 ERP platform. It is maintained for developers, DevOps engineers,
QA engineers, business analysts, and future system maintainers.

--------------------------------------------------------------------------------
1. PROJECT OVERVIEW
--------------------------------------------------------------------------------
* Platform Name: PLUS33 ERP
* Description: A robust, multi-tenant capable Enterprise Resource Planning (ERP)
  system designed to model organizational structures, manage workforce
  schedules and payroll, track real-time inventory levels, control procurement 
  and supply chain workflows, and perform finance accounting ledgers.
* High-Level Architecture: Layered Monolith Architecture following standard
  Spring Boot conventions: Controller -> DTO -> Mapper -> Service ->
  Repository -> JPA Entity.
* Technology Stack:
  - Java Version: JDK 17
  - Backend Framework: Spring Boot 4.0.8-SNAPSHOT, Spring Security, Spring Data JPA
  - Database: PostgreSQL (for production, staging, and integration test execution)
  - Database Migrations: Flyway (with postgresql-specific extensions)
  - Mapping: MapStruct 1.6.3
  - Lombok for boilerplate minimization
  - Nimbus JOSE JWT for stateless JWT decoding and signature verification
  - Testing: JUnit 5, MockMvc, AssertJ, Hamcrest Matchers
  - Build System: Apache Maven (wrapper scripts provided)

--------------------------------------------------------------------------------
2. MODULE OVERVIEW
--------------------------------------------------------------------------------
The application is structured into the following operational domains:

* Authentication & Authorization:
  Secures APIs using stateless JWT authentication. Resolves user roles and maps
  granted authority strings to controllers for method-level security checking.

* Organization Management:
  Represents company hierarchy and nodes:
  - Companies: Top-level logical tenant boundaries.
  - Regions: Geographical group boundaries within a company.
  - Warehouses: Physical storage nodes containing bulk inventory.
  - Stores: Retail outlets that pull stock from default warehouses.

* Workforce Management:
  Manages employee profiles, work shifts, daily clock-in/out attendance logs,
  leave type policies, leave applications, payroll periods, and calculations
  supporting overtime multipliers.

* Inventory Management & Traceability:
  Supports product catalog metadata, real-time stock balances (physical quantity and reserved quantity),
  audit trails of stock movements, manual stock corrections, and physical stock count cycle sessions.
  Also includes traceability: batch/lot expiry tracking, serial number tracking, genealogy movement logs,
  and dedicated quarantine/recall workflows.

* Procurement:
  Streamlines supply chain interactions from Purchase Requests (PRs), Supplier
  information directories, Purchase Orders (POs), and Goods Receipts (GRs)
  validating actual received physical quantities against orders.

* Finance & Ledger:
  Implements double-entry ledger bookkeeping. Defines the Chart of Accounts (COA)
  mapping codes (e.g., Accounts Payable 2100, Cash 1100, Bank 1200), Journal Entries
  representing balanced debits/credits, Supplier Invoices with three-way matching,
  and Supplier Payments allocating cash offsets.

* Analytics & Reporting:
  Uses PostgreSQL Materialized Views to perform concurrent, high-performance updates
  on key metrics like PO fulfillment, aging payables, supplier performance, and
  three-way matching exceptions. Also provides inventory dashboards for KPIs,
  stock aging/expiry, ABC/XYZ classifications, slow/dead stock, replenishment rule coverage,
  and recalls.

* Sales & Customer Management:
  Supports customer accounts (B2B and B2C), billing/shipping addresses,
  tax profile categories, credit limit thresholds, outstanding balances,
  pricing tiers, payment terms, and status lifecycles. Includes sales
  orders with snapshot-based customer data, credit limit validation,
  and idempotency. Implements order fulfillment through pick lists with
  stock allocation, partial picking, packing, shipping, and cancellation
  rollback workflows.

--------------------------------------------------------------------------------
3. DATABASE DOCUMENTATION
--------------------------------------------------------------------------------
This section lists the database tables, purposes, key relationships, constraints,
and corresponding Flyway migration files:

* roles
  - Purpose: Stores system security roles.
  - Key Fields: id (BIGSERIAL PK), code (VARCHAR(50) UNIQUE), name (VARCHAR(100)).
  - Flyway Version: V2__initial_schema.sql

* permissions
  - Purpose: Granular security actions.
  - Key Fields: id (BIGSERIAL PK), code (VARCHAR(50) UNIQUE), name (VARCHAR(100)).
  - Flyway Version: V2__initial_schema.sql

* role_permissions
  - Purpose: Joins security roles to permissions.
  - Relationships: FK role_id -> roles, FK permission_id -> permissions.
  - Constraints: UNIQUE (role_id, permission_id).
  - Flyway Version: V2__initial_schema.sql

* users
  - Purpose: System user profiles.
  - Key Fields: id (BIGSERIAL PK), email (VARCHAR(100) UNIQUE), password (VARCHAR(100)).
  - Flyway Version: V4__create_users_table.sql

* user_roles
  - Purpose: Joins users to roles.
  - Relationships: FK user_id -> users, FK role_id -> roles.
  - Constraints: UNIQUE (user_id, role_id).
  - Flyway Version: V4__create_users_table.sql

* companies
  - Purpose: Identifies top-level company entities.
  - Key Fields: id (BIGSERIAL PK), code (VARCHAR(50) UNIQUE), active (BOOLEAN).
  - Flyway Version: V7__create_organization_structure.sql

* regions
  - Purpose: Groups locations by region under a company.
  - Relationships: FK company_id -> companies.
  - Flyway Version: V7__create_organization_structure.sql

* warehouses
  - Purpose: Models bulk storage warehouses.
  - Relationships: FK region_id -> regions.
  - Constraints: Unique code.
  - Flyway Version: V7__create_organization_structure.sql

* stores
  - Purpose: Models store locations pulling default stock.
  - Relationships: FK region_id -> regions, FK default_warehouse_id -> warehouses.
  - Flyway Version: V7__create_organization_structure.sql

* user_regions / user_warehouses / user_stores
  - Purpose: Maps users to their managed regions, warehouses, or stores.
  - Flyway Version: V9__create_user_assignments.sql

* employees
  - Purpose: Workforce member profiles.
  - Constraints: Unique employee_code, unique email.
  - Flyway Version: V10__create_employees.sql / V28__migrate_employee_uniqueness.sql

* shifts
  - Purpose: Shift schedule details (start time, end time).
  - Flyway Version: V11__create_shifts.sql

* employee_shifts
  - Purpose: Shift assignments for employees.
  - Flyway Version: V11__create_shifts.sql

* attendance
  - Purpose: Daily timekeeping logs (clock-in, clock-out).
  - Relationships: FK employee_id -> employees.
  - Flyway Version: V12__create_attendance.sql

* leave_types
  - Purpose: Types of leaves (Annual, Sick, Maternity).
  - Flyway Version: V13__create_leave_management.sql

* employee_leaves
  - Purpose: Tracks employee leave request state.
  - Flyway Version: V13__create_leave_management.sql

* payroll_periods
  - Purpose: Defines start and end dates for payroll computations.
  - Flyway Version: V14__create_payroll.sql

* employee_payrolls
  - Purpose: Computed earnings, taxes, deductions, net salary.
  - Flyway Version: V14__create_payroll.sql

* overtime_rules
  - Purpose: Configuration of overtime rates.
  - Flyway Version: V14__create_payroll.sql

* product_categories
  - Purpose: Product groupings.
  - Flyway Version: V15__create_inventory.sql

* units_of_measure
  - Purpose: Metric abbreviations (e.g. PCS, KG, LTR).
  - Flyway Version: V15__create_inventory.sql

* products
  - Purpose: Inventory product metadata.
  - Relationships: FK category_id -> product_categories, FK uom_id -> units_of_measure.
  - Flyway Version: V15__create_inventory.sql

* inventory_stock
  - Purpose: Tracks on-hand inventory levels and reservations.
  - Key Fields: quantity (DECIMAL), reserved_quantity (DECIMAL).
  - Constraints: CHECK (reserved_quantity >= 0 AND reserved_quantity <= quantity).
  - Flyway Version: V15__create_inventory.sql / V39__create_inventory_transfers.sql

* stock_movements
  - Purpose: Audit trail of stock additions, deductions, and reservations.
  - Key Fields: movement_type (VARCHAR), reference_type (VARCHAR).
  - Flyway Version: V15__create_inventory.sql

* suppliers
  - Purpose: Scoped supplier entities.
  - Relationships: FK company_id -> companies.
  - Flyway Version: V16__create_procurement.sql / V24__migrate_suppliers_to_company_scope.sql

* purchase_requests
  - Purpose: Requisition demands.
  - Constraints: chk_purchase_requests_location CHECK (
      (warehouse_id IS NOT NULL AND store_id IS NULL) OR
      (warehouse_id IS NULL AND store_id IS NOT NULL)
    ).
  - Flyway Version: V16__create_procurement.sql / V30__alter_purchase_requests_schema.sql

* purchase_request_items
  - Purpose: Line items under a Purchase Request.
  - Flyway Version: V16__create_procurement.sql

* purchase_orders
  - Purpose: Standard PO entries.
  - Relationships: FK company_id -> companies, FK supplier_id -> suppliers, FK purchase_request_id -> purchase_requests.
  - Flyway Version: V16__create_procurement.sql / V32__alter_purchase_orders_schema.sql

* purchase_order_items
  - Purpose: Line items under a Purchase Order.
  - Flyway Version: V16__create_procurement.sql

* goods_receipts
  - Purpose: Documents actual arrivals.
  - Relationships: FK purchase_order_id -> purchase_orders.
  - Flyway Version: V33__create_goods_receipts_schema.sql

* goods_receipt_items
  - Purpose: Received line quantities.
  - Flyway Version: V33__create_goods_receipts_schema.sql

* chart_of_accounts
  - Purpose: Financial ledger account codes.
  - Constraints: UNIQUE (company_id, account_code).
  - Flyway Version: V19__create_finance.sql

* journal_entries
  - Purpose: General ledger entries.
  - Relationships: FK company_id -> companies, FK reversal_entry_id -> journal_entries.
  - Flyway Version: V19__create_finance.sql

* journal_entry_lines
  - Purpose: Debit or Credit line offsets.
  - Constraints: Debit/Credit totals must balance per entry.
  - Flyway Version: V19__create_finance.sql

* supplier_invoices
  - Purpose: Bills submitted by suppliers.
  - Flyway Version: V19__create_finance.sql

* supplier_invoice_items
  - Purpose: Invoiced quantities and prices matched to PO items.
  - Constraints: UNIQUE (supplier_invoice_id, purchase_order_item_id)
    DEFERRABLE INITIALLY DEFERRED.
  - Flyway Version: V35__alter_supplier_invoices_schema.sql / V41__fix_invoice_item_constraint_deferrable.sql

* payments
  - Purpose: Outward payments.
  - Relationships: FK company_id -> companies, FK journal_entry_id -> journal_entries.
  - Flyway Version: V19__create_finance.sql / V37__alter_payments_schema.sql

* payment_allocations
  - Purpose: Allocates paid cash offsets against invoices.
  - Flyway Version: V19__create_finance.sql

* inventory_transfers
  - Purpose: Records warehouse-to-store or warehouse-to-warehouse transfers.
  - Constraints:
    - chk_transfers_source CHECK ((source_warehouse_id IS NOT NULL AND source_store_id IS NULL) OR (source_warehouse_id IS NULL AND source_store_id IS NOT NULL))
    - chk_transfers_dest CHECK ((dest_warehouse_id IS NOT NULL AND dest_store_id IS NULL) OR (dest_warehouse_id IS NULL AND dest_store_id IS NOT NULL))
    - Trigger trg_validate_inventory_transfer blocks store-to-warehouse transfers, same-location transfers, and mismatched companies.
  - Flyway Version: V39__create_inventory_transfers.sql

* inventory_transfer_items
  - Purpose: Transfer line items with received tracking.
  - Constraints: CHECK (received_quantity <= quantity).
  - Flyway Version: V39__create_inventory_transfers.sql

* inventory_adjustments
  - Purpose: Records manual stock corrections, damages, expired goods, and count variances.
  - Constraints:
    - chk_adjustments_status CHECK (status IN ('DRAFT', 'SUBMITTED', 'APPROVED', 'POSTED', 'CANCELLED'))
    - chk_adjustments_type CHECK (adjustment_type IN ('DAMAGE', 'EXPIRED', 'SHRINKAGE', 'FOUND_STOCK', 'STOCK_COUNT_VARIANCE', 'MANUAL_CORRECTION'))
    - chk_adjustments_location CHECK ((warehouse_id IS NOT NULL AND store_id IS NULL) OR (warehouse_id IS NULL AND store_id IS NOT NULL))
    - Trigger trg_validate_inventory_adjustment blocks locations belonging to different companies.
  - Flyway Version: V42__create_inventory_adjustments.sql

* inventory_adjustment_items
  - Purpose: Individual product quantities adjusted under a header.
  - Constraints: UNIQUE (adjustment_id, product_id) DEFERRABLE INITIALLY DEFERRED, CHECK (quantity <> 0.00).
  - Flyway Version: V42__create_inventory_adjustments.sql / V43__seed_inventory_adjustment_permissions.sql

* stock_counts
  - Purpose: Tracks physical count sessions at a specific store or warehouse.
  - Constraints: XOR location, uq_stock_count_item, region-company match trigger, active count uniqueness.
  - Flyway Version: V44__create_stock_counts.sql / V45__seed_stock_count_permissions.sql

* stock_count_items
  - Purpose: Individual product lines within a count session.
  - Constraints: UNIQUE (stock_count_id, product_id) DEFERRABLE INITIALLY DEFERRED.
  - Flyway Version: V44__create_stock_counts.sql

* recipes / recipe_items / sales_transactions / sales_transaction_items / waste_records
  - Purpose: Tracks retail kitchen bills of materials, point of sales transactions, and waste.
  - Flyway Version: V18__create_store_operations.sql

* stock_transfers / stock_transfer_items / stock_adjustments
  - Purpose: Core warehouse logistics stock tracking.
  - Flyway Version: V17__create_warehouse_operations.sql

* dashboard_configs
  - Purpose: Configuration for analytical dashboard displays.
  - Flyway Version: V21__create_analytics.sql

* inventory_lots
  - Purpose: Tracks manufacturing batches, lot numbers, and expiry dates.
  - Relationships: FK company_id -> companies, FK product_id -> products.
  - Constraints: UNIQUE (company_id, product_id, lot_number).
  - Flyway Version: V48__create_inventory_traceability.sql

* inventory_serials
  - Purpose: Tracks individual products by serial numbers and location snapshot.
  - Relationships: FK company_id -> companies, FK product_id -> products, FK lot_id -> inventory_lots.
  - Constraints: UNIQUE (company_id, product_id, serial_number), location XOR constraint.
  - Flyway Version: V48__create_inventory_traceability.sql

* inventory_recalls
  - Purpose: Logs product recall campaigns and isolates compromised lots/serials.
  - Relationships: FK company_id -> companies, FK product_id -> products, FK lot_id -> inventory_lots, FK serial_id -> inventory_serials.
  - Constraints: Unique recall_number, unique active lot/serial indexes to prevent overlapping.
  - Flyway Version: V48__create_inventory_traceability.sql / V49__seed_inventory_traceability_permissions.sql

* inventory_trace_events
  - Purpose: High-integrity ledger tracking the end-to-end genealogy of all items.
  - Relationships: FK company_id -> companies, FK product_id -> products, FK lot_id -> inventory_lots, FK serial_id -> inventory_serials.
  - Flyway Version: V48__create_inventory_traceability.sql

* customers
  - Purpose: Stores customer profiles, addresses, credit limits, and pricing tiers.
  - Relationships: FK company_id -> companies.
  - Constraints: UNIQUE (company_id, code), UNIQUE (company_id, email), checks on credit_limit, outstanding_balance, discount_rate, and payment_terms_days.
  - Flyway Version: V52__create_sales_customers.sql / V53__seed_sales_customer_permissions.sql

* sales_orders
  - Purpose: Serves as the source document for sales agreements, tracking headers and historical customer snapshots.
  - Relationships: FK company_id -> companies, FK customer_id -> customers, FK ordered_by -> users, FK submitted_by -> users, FK approved_by -> users, FK cancelled_by -> users.
  - Constraints: UNIQUE (company_id, order_number), UNIQUE (company_id, client_reference_id), CHECK on status, CHECKs on non-negative subtotal, discount_amount, tax_amount, total_amount, and outstanding_amount.
  - Flyway Version: V54__create_sales_orders.sql / V55__seed_sales_order_permissions.sql

* sales_order_items
  - Purpose: Tracks individual product quantities, unit prices, discounts, taxes, and totals under a sales order header.
  - Key Fields: allocated_quantity, fulfilled_quantity, invoiced_quantity (tracked by downstream modules).
  - Relationships: FK sales_order_id -> sales_orders, FK product_id -> products.
  - Constraints: UNIQUE (sales_order_id, product_id) DEFERRABLE INITIALLY DEFERRED, CHECKs on ordered_quantity > 0, unit_price >= 0, discount_percentage (0-100), tax_percentage (0-100), and line_total >= 0.
  - Flyway Version: V54__create_sales_orders.sql

* pick_lists
  - Purpose: Manages order fulfillment pick lists linking a sales order to a warehouse or store for stock allocation and shipping.
  - Key Fields: id (BIGSERIAL PK), company_id, sales_order_id, pick_number (VARCHAR(50)), client_reference_id (UUID), status (PickListStatus enum), warehouse_id (nullable), store_id (nullable), created_by, released_by, released_at, picked_by, picked_at, packed_by, packed_at, shipped_by, shipped_at, cancelled_by, cancelled_at, cancellation_reason, version.
  - Relationships: FK company_id -> companies, FK sales_order_id -> sales_orders, FK warehouse_id -> warehouses, FK store_id -> stores, FK created_by/released_by/picked_by/packed_by/shipped_by/cancelled_by -> users.
  - Constraints: UNIQUE (company_id, client_reference_id), CHECK on status (DRAFT, RELEASED, PICKING, PICKED, PACKED, SHIPPED, CANCELLED), XOR constraint on warehouse_id/store_id.
  - Flyway Version: V56__create_pick_lists.sql / V57__seed_pick_list_permissions.sql

* pick_list_items
  - Purpose: Individual line items on a pick list tracking ordered, allocated, picked, and shipped quantities.
  - Key Fields: id (BIGSERIAL PK), pick_list_id, sales_order_item_id, product_id, ordered_quantity, allocated_quantity, picked_quantity, shipped_quantity, version.
  - Relationships: FK pick_list_id -> pick_lists, FK sales_order_item_id -> sales_order_items, FK product_id -> products.
  - Constraints: UNIQUE (pick_list_id, product_id) DEFERRABLE INITIALLY DEFERRED, CHECKs on quantities >= 0.
  - Flyway Version: V56__create_pick_lists.sql

* inventory_allocations
  - Purpose: Audit trail for stock reservations linking pick lists to specific inventory locations with lifecycle status tracking.
  - Key Fields: id (BIGSERIAL PK), company_id, sales_order_id, sales_order_item_id, pick_list_id, product_id, warehouse_id (nullable), store_id (nullable), allocated_quantity, allocation_status (ACTIVE, RELEASED, CONSUMED, CANCELLED), allocation_reference.
  - Relationships: FK company_id -> companies, FK sales_order_id -> sales_orders, FK sales_order_item_id -> sales_order_items, FK pick_list_id -> pick_lists, FK product_id -> products, FK warehouse_id -> warehouses, FK store_id -> stores.
  - Constraints: CHECKs on allocated_quantity > 0 and allocation_status enum values.
  - Flyway Version: V56__create_pick_lists.sql

* customer_invoices
  - Purpose: Stores billing invoices sent to customers.
  - Relationships: FK company_id -> companies, FK customer_id -> customers, FK sales_order_id -> sales_orders, FK journal_entry_id -> journal_entries, FK created_by/submitted_by/approved_by/cancelled_by -> users.
  - Constraints: UNIQUE (company_id, invoice_number), UNIQUE (company_id, client_reference_id), CHECKs on status enum, and non-negative subtotal, discount, tax, total, paid, and outstanding balances.
  - Flyway Version: V58__create_customer_invoices.sql / V59__seed_customer_invoice_permissions.sql

* customer_invoice_items
  - Purpose: Line items under a customer invoice.
  - Relationships: FK customer_invoice_id -> customer_invoices, FK sales_order_item_id -> sales_order_items, FK pick_list_item_id -> pick_list_items, FK product_id -> products.
  - Constraints: UNIQUE (customer_invoice_id, product_id) DEFERRABLE INITIALLY DEFERRED, CHECKs on quantity > 0, unit_price >= 0, discount_percentage (0-100), tax_percentage (0-100), and line totals.
  - Flyway Version: V58__create_customer_invoices.sql

--------------------------------------------------------------------------------
4. ENTITY DOCUMENTATION
--------------------------------------------------------------------------------
All key database tables correspond to JPA entity classes annotated with Lombok
builders, getters/setters, and validation rules.

* User
  - Location: com.plus33.erp.security.entity.User
  - Fields: id (Long), email (String), password (String), roles (Set<Role>)
  - Validations: @Email, @NotBlank

* Role
  - Location: com.plus33.erp.security.entity.Role
  - Fields: id (Long), code (String), name (String), permissions (Set<Permission>)

* Company
  - Location: com.plus33.erp.organization.entity.Company
  - Fields: id (Long), code (String), name (String), active (Boolean)

* Region
  - Location: com.plus33.erp.organization.entity.Region
  - Fields: id (Long), code (String), name (String), company (Company)

* Warehouse
  - Location: com.plus33.erp.organization.entity.Warehouse
  - Fields: id (Long), code (String), name (String), region (Region), active (Boolean)

* Store
  - Location: com.plus33.erp.organization.entity.Store
  - Fields: id (Long), code (String), name (String), region (Region), defaultWarehouse (Warehouse), active (Boolean)

* Employee
  - Location: com.plus33.erp.workforce.entity.Employee
  - Fields: id (Long), employeeCode (String), firstName (String), lastName (String), email (String), active (Boolean)
  - Validations: @NotBlank, @Email

* Product
  - Location: com.plus33.erp.inventory.entity.Product
  - Fields: id (Long), code (String), name (String), category (ProductCategory), uom (UnitOfMeasure), active (Boolean)

* InventoryStock
  - Location: com.plus33.erp.inventory.entity.InventoryStock
  - Fields: id (Long), product (Product), warehouse (Warehouse), store (Store), quantity (BigDecimal), reservedQuantity (BigDecimal)
  - Validations: quantity >= 0, reservedQuantity <= quantity

* StockMovement
  - Location: com.plus33.erp.inventory.entity.StockMovement
  - Fields: id (Long), product (Product), warehouse (Warehouse), store (Store), quantity (BigDecimal), movementType (String), referenceType (StockMovementReferenceType), referenceId (Long), referenceNumber (String)

* Supplier
  - Location: com.plus33.erp.procurement.entity.Supplier
  - Fields: id (Long), code (String), name (String), company (Company), active (Boolean)

* PurchaseRequest
  - Location: com.plus33.erp.procurement.entity.PurchaseRequest
  - Fields: id (Long), requestNumber (String), company (Company), supplier (Supplier), warehouse (Warehouse), store (Store), status (PurchaseRequestStatus), requiredDate (LocalDate), items (List<PurchaseRequestItem>)
  - Validations: XOR constraint on destination location, requiredDate must be future/present

* PurchaseRequestItem
  - Location: com.plus33.erp.procurement.entity.PurchaseRequestItem
  - Fields: id (Long), purchaseRequest (PurchaseRequest), product (Product), requestedQuantity (BigDecimal), approvedQuantity (BigDecimal)

* PurchaseOrder
  - Location: com.plus33.erp.procurement.entity.PurchaseOrder
  - Fields: id (Long), orderNumber (String), company (Company), supplier (Supplier), purchaseRequest (PurchaseRequest), status (PurchaseOrderStatus), totalAmount (BigDecimal), items (List<PurchaseOrderItem>)

* PurchaseOrderItem
  - Location: com.plus33.erp.procurement.entity.PurchaseOrderItem
  - Fields: id (Long), purchaseOrder (PurchaseOrder), product (Product), orderedQuantity (BigDecimal), receivedQuantity (BigDecimal), remainingQuantity (BigDecimal), invoicedQuantity (BigDecimal)

* GoodsReceipt
  - Location: com.plus33.erp.procurement.entity.GoodsReceipt
  - Fields: id (Long), receiptNumber (String), purchaseOrder (PurchaseOrder), status (GoodsReceiptStatus), receivedDate (LocalDate), items (List<GoodsReceiptItem>)

* GoodsReceiptItem
  - Location: com.plus33.erp.procurement.entity.GoodsReceiptItem
  - Fields: id (Long), goodsReceipt (GoodsReceipt), purchaseOrderItem (PurchaseOrderItem), receivedQuantity (BigDecimal)

* Account
  - Location: com.plus33.erp.finance.entity.Account
  - Fields: id (Long), accountCode (String), accountName (String), accountType (String), company (Company)

* JournalEntry
  - Location: com.plus33.erp.finance.entity.JournalEntry
  - Fields: id (Long), entryNumber (String), company (Company), entryDate (LocalDate), status (String), lines (List<JournalEntryLine>), reversalEntry (JournalEntry)

* JournalEntryLine
  - Location: com.plus33.erp.finance.entity.JournalEntryLine
  - Fields: id (Long), journalEntry (JournalEntry), account (Account), debitAmount (BigDecimal), creditAmount (BigDecimal)

* SupplierInvoice
  - Location: com.plus33.erp.finance.entity.SupplierInvoice
  - Fields: id (Long), invoiceNumber (String), company (Company), supplier (Supplier), purchaseOrder (PurchaseOrder), totalAmount (BigDecimal), outstandingBalance (BigDecimal), paidAmount (BigDecimal), status (SupplierInvoiceStatus), items (List<SupplierInvoiceItem>)

* SupplierInvoiceItem
  - Location: com.plus33.erp.finance.entity.SupplierInvoiceItem
  - Fields: id (Long), supplierInvoice (SupplierInvoice), purchaseOrderItem (PurchaseOrderItem), product (Product), quantity (BigDecimal), unitPrice (BigDecimal)

* Payment
  - Location: com.plus33.erp.finance.entity.Payment
  - Fields: id (Long), paymentNumber (String), company (Company), supplier (Supplier), amount (BigDecimal), status (PaymentStatus), allocations (List<PaymentAllocation>), journalEntry (JournalEntry)

* PaymentAllocation
  - Location: com.plus33.erp.finance.entity.PaymentAllocation
  - Fields: id (Long), payment (Payment), supplierInvoice (SupplierInvoice), allocatedAmount (BigDecimal)

* InventoryTransfer
  - Location: com.plus33.erp.inventory.entity.InventoryTransfer
  - Fields: id (Long), transferNumber (String), company (Company), sourceWarehouse (Warehouse), sourceStore (Store), destWarehouse (Warehouse), destStore (Store), status (InventoryTransferStatus), items (List<InventoryTransferItem>)

* InventoryTransferItem
  - Location: com.plus33.erp.inventory.entity.InventoryTransferItem
  - Fields: id (Long), inventoryTransfer (InventoryTransfer), product (Product), quantity (BigDecimal), receivedQuantity (BigDecimal), remainingQuantity (BigDecimal)

* InventoryAdjustment
  - Location: com.plus33.erp.inventory.entity.InventoryAdjustment
  - Fields: id (Long), adjustmentNumber (String), company (Company), warehouse (Warehouse), store (Store), adjustmentType (InventoryAdjustmentType), status (InventoryAdjustmentStatus), clientReferenceId (UUID), remarks (String), items (List<InventoryAdjustmentItem>)

* InventoryAdjustmentItem
  - Location: com.plus33.erp.inventory.entity.InventoryAdjustmentItem
  - Fields: id (Long), inventoryAdjustment (InventoryAdjustment), product (Product), quantity (BigDecimal)

* StockCount
  - Location: com.plus33.erp.inventory.entity.StockCount
  - Fields: id (Long), countNumber (String), company (Company), warehouse (Warehouse), store (Store), status (StockCountStatus), countType (StockCountType), blindCount (boolean), assignedTo (User), adjustment (InventoryAdjustment), approvalRequired (boolean), approvalThresholdPercentage (BigDecimal), clientReferenceId (UUID), remarks (String), rejectionReason (String), recountNumber (int), items (List<StockCountItem>)

* StockCountItem
  - Location: com.plus33.erp.inventory.entity.StockCountItem
  - Fields: id (Long), stockCount (StockCount), product (Product), systemQuantity (BigDecimal), reservedQuantity (BigDecimal), availableQuantity (BigDecimal), countedQuantity (BigDecimal), variance (BigDecimal)

* InventoryLot
  - Location: com.plus33.erp.inventory.entity.InventoryLot
  - Fields: id (Long), company (Company), product (Product), lotNumber (String), expiryDate (LocalDate), manufacturedDate (LocalDate), status (InventoryLotStatus), version (Long)

* InventorySerial
  - Location: com.plus33.erp.inventory.entity.InventorySerial
  - Fields: id (Long), company (Company), product (Product), lot (InventoryLot), serialNumber (String), warehouse (Warehouse), store (Store), status (InventorySerialStatus), version (Long)

* InventoryRecall
  - Location: com.plus33.erp.inventory.entity.InventoryRecall
  - Fields: id (Long), company (Company), product (Product), lot (InventoryLot), serial (InventorySerial), recallNumber (String), recallReason (String), recallReferenceNumber (String), recalledBy (User), recalledAt (LocalDateTime)

* InventoryTraceEvent
  - Location: com.plus33.erp.inventory.entity.InventoryTraceEvent
  - Fields: id (Long), company (Company), product (Product), lot (InventoryLot), serial (InventorySerial), warehouse (Warehouse), store (Store), eventType (InventoryTraceEventType), quantity (BigDecimal), referenceType (InventoryTraceReferenceType), referenceId (Long), referenceNumber (String), notes (String), createdBy (User), createdAt (LocalDateTime)

* Customer
  - Location: com.plus33.erp.sales.entity.Customer
  - Fields: id (Long), company (Company), code (String), name (String), customerType (CustomerType), status (CustomerStatus), contactPerson (String), email (String), phone (String), billingAddress (String), shippingAddress (String), taxNumber (String), taxProfile (TaxProfile), creditLimit (BigDecimal), outstandingBalance (BigDecimal), pricingTier (String), discountRate (BigDecimal), paymentTermsDays (Integer), currencyCode (String), createdAt (LocalDateTime), updatedAt (LocalDateTime), version (Long)

* SalesOrder
  - Location: com.plus33.erp.sales.entity.SalesOrder
  - Fields: id (Long), company (Company), customer (Customer), orderNumber (String), clientReferenceId (UUID), orderDate (LocalDate), requestedDeliveryDate (LocalDate), currencyCode (String), paymentTermsDays (Integer), billingAddress (String), shippingAddress (String), status (SalesOrderStatus), customerName (String), customerCode (String), customerType (String), pricingTier (String), discountRate (BigDecimal), taxProfile (String), subtotal (BigDecimal), discountAmount (BigDecimal), taxAmount (BigDecimal), totalAmount (BigDecimal), outstandingAmount (BigDecimal), creditOverride (Boolean), orderedBy (User), submittedBy (User), approvedBy (User), cancelledBy (User), submittedAt (LocalDateTime), approvedAt (LocalDateTime), cancelledAt (LocalDateTime), cancellationReason (String), createdAt (LocalDateTime), updatedAt (LocalDateTime), version (Long), items (List<SalesOrderItem>)

* SalesOrderItem
  - Location: com.plus33.erp.sales.entity.SalesOrderItem
  - Fields: id (Long), salesOrder (SalesOrder), product (Product), orderedQuantity (BigDecimal), unitPrice (BigDecimal), discountPercentage (BigDecimal), taxPercentage (BigDecimal), lineTotal (BigDecimal), allocatedQuantity (BigDecimal), fulfilledQuantity (BigDecimal), invoicedQuantity (BigDecimal), version (Long)

* PickList
  - Location: com.plus33.erp.sales.entity.PickList
  - Fields: id (Long), company (Company), salesOrder (SalesOrder), pickNumber (String), clientReferenceId (UUID), status (PickListStatus), warehouse (Warehouse), store (Store), createdBy (User), releasedBy (User), releasedAt (LocalDateTime), pickedBy (User), pickedAt (LocalDateTime), packedBy (User), packedAt (LocalDateTime), shippedBy (User), shippedAt (LocalDateTime), cancelledBy (User), cancelledAt (LocalDateTime), cancellationReason (String), version (Long), items (List<PickListItem>)

* PickListItem
  - Location: com.plus33.erp.sales.entity.PickListItem
  - Fields: id (Long), pickList (PickList), salesOrderItem (SalesOrderItem), product (Product), orderedQuantity (BigDecimal), allocatedQuantity (BigDecimal), pickedQuantity (BigDecimal), shippedQuantity (BigDecimal), version (Long)

* InventoryAllocation
  - Location: com.plus33.erp.sales.entity.InventoryAllocation
  - Fields: id (Long), company (Company), salesOrder (SalesOrder), salesOrderItem (SalesOrderItem), pickList (PickList), product (Product), warehouse (Warehouse), store (Store), allocatedQuantity (BigDecimal), allocationStatus (AllocationStatus), allocationReference (String)

* CustomerInvoice
  - Location: com.plus33.erp.sales.entity.CustomerInvoice
  - Fields: id (Long), company (Company), customer (Customer), salesOrder (SalesOrder), invoiceNumber (String), clientReferenceId (UUID), invoiceDate (LocalDate), dueDate (LocalDate), currencyCode (String), subtotalAmount (BigDecimal), taxAmount (BigDecimal), discountAmount (BigDecimal), totalAmount (BigDecimal), paidAmount (BigDecimal), outstandingBalance (BigDecimal), status (CustomerInvoiceStatus), journalEntry (JournalEntry), createdBy (User), submittedBy (User), approvedBy (User), cancelledBy (User), createdAt (LocalDateTime), submittedAt (LocalDateTime), approvedAt (LocalDateTime), cancelledAt (LocalDateTime), cancellationReason (String), items (List<CustomerInvoiceItem>), version (Long)

* CustomerInvoiceItem
  - Location: com.plus33.erp.sales.entity.CustomerInvoiceItem
  - Fields: id (Long), customerInvoice (CustomerInvoice), salesOrderItem (SalesOrderItem), pickListItem (PickListItem), product (Product), quantity (BigDecimal), unitPrice (BigDecimal), discountPercentage (BigDecimal), taxPercentage (BigDecimal), netAmount (BigDecimal), taxAmount (BigDecimal), discountAmount (BigDecimal), totalAmount (BigDecimal), version (Long)

--------------------------------------------------------------------------------
5. API DOCUMENTATION
--------------------------------------------------------------------------------
This section lists primary REST endpoints, parameters, permissions, validation rules,
workflow transitions, and possible errors:

* Authentication (AuthController)
  - Endpoint: POST /api/v1/auth/login
  - Purpose: Validates email/password credentials and issues a JWT token.
  - Required Permission: None
  - Request: LoginRequest DTO (email, password)
  - Response: TokenResponse (token, email, roles)
  - Errors: 400 Bad Request (validation errors), 401 Unauthorized (invalid credentials)

* Supplier Invoices (SupplierInvoiceController)
  - Endpoint: POST /api/v1/supplier-invoices
  - Purpose: Creates a draft supplier invoice linked to a PO.
  - Required Permission: SUPPLIER_INVOICE_CREATE
  - Request: SupplierInvoiceRequest DTO
  - Response: ApiResponse<SupplierInvoiceResponse>
  - Errors: 400 (validation), 404 (not found), 409 (conflict)

  - Endpoint: PUT /api/v1/supplier-invoices/{id}
  - Purpose: Updates a draft supplier invoice.
  - Required Permission: SUPPLIER_INVOICE_UPDATE
  - Request: SupplierInvoiceUpdateRequest DTO
  - Response: ApiResponse<SupplierInvoiceResponse>
  - Errors: 400 (invalid state/validation), 404 (not found)

  - Endpoint: POST /api/v1/supplier-invoices/{id}/approve
  - Purpose: Approves a draft invoice. Transitions status to APPROVED and automatically generates posted ledger journal entries.
  - Required Permission: SUPPLIER_INVOICE_APPROVE
  - Request: None
  - Response: ApiResponse<SupplierInvoiceResponse>
  - Errors: 400 (not DRAFT), 404 (not found)

  - Endpoint: POST /api/v1/supplier-invoices/{id}/cancel
  - Purpose: Cancels a supplier invoice and automatically generates reversing journal entries.
  - Required Permission: SUPPLIER_INVOICE_CANCEL
  - Request: None
  - Response: ApiResponse<SupplierInvoiceResponse>
  - Errors: 400 (not APPROVED), 404 (not found)

* Payment Processing (PaymentController)
  - Endpoint: POST /api/v1/payments
  - Purpose: Creates a completed payment, records allocations against outstanding supplier invoices, and generates posted cash/AP journal entries.
  - Required Permission: PAYMENT_CREATE
  - Request: PaymentRequest DTO (companyId, supplierId, allocations, paymentMethod, amount, referenceNumber)
  - Response: ApiResponse<PaymentResponse>
  - Errors: 400 (validation, allocation limits exceeded), 404 (not found), 409 (duplicate reference)

  - Endpoint: POST /api/v1/payments/{id}/cancel
  - Purpose: Cancels a payment, restores invoice outstanding balances, and generates reversing journal entries.
  - Required Permission: PAYMENT_CANCEL
  - Request: PaymentCancelRequest DTO (reason)
  - Response: ApiResponse<PaymentResponse>
  - Errors: 400 (not COMPLETED), 404 (not found)

* Inventory Transfers (InventoryTransferController)
  - Endpoint: POST /api/v1/inventory-transfers
  - Purpose: Initiates a draft internal stock transfer request.
  - Required Permission: INVENTORY_TRANSFER_CREATE
  - Request: InventoryTransferRequest DTO (companyId, source, destination, items)
  - Response: ApiResponse<InventoryTransferResponse>
  - Errors: 400 (validation, store-to-warehouse transfer, identical locations)

  - Endpoint: POST /api/v1/inventory-transfers/{id}/submit
  - Purpose: Submits a draft transfer. Transitions status to SUBMITTED.
  - Required Permission: INVENTORY_TRANSFER_UPDATE
  - Errors: 400 (not DRAFT), 404 (not found)

  - Endpoint: POST /api/v1/inventory-transfers/{id}/approve
  - Purpose: Approves a submitted transfer. Locks stock at source (adds reserved_quantity) and transitions status to APPROVED.
  - Required Permission: INVENTORY_TRANSFER_APPROVE
  - Errors: 400 (not SUBMITTED, insufficient available stock), 404 (not found)

  - Endpoint: POST /api/v1/inventory-transfers/{id}/dispatch
  - Purpose: Dispatches an approved transfer. Deducts physical and reserved quantities from source stock and transitions status to IN_TRANSIT.
  - Required Permission: INVENTORY_TRANSFER_DISPATCH
  - Errors: 400 (not APPROVED)

  - Endpoint: POST /api/v1/inventory-transfers/{id}/receive
  - Purpose: Completes a transfer. Increments destination stock, marks received quantities, and transitions status to RECEIVED and CLOSED.
  - Required Permission: INVENTORY_TRANSFER_RECEIVE
  - Errors: 400 (not IN_TRANSIT)

  - Endpoint: POST /api/v1/inventory-transfers/{id}/cancel
  - Purpose: Cancels a transfer. Releases reserved stock at source and transitions status to CANCELLED.
  - Required Permission: INVENTORY_TRANSFER_CANCEL
  - Errors: 400 (already dispatched / IN_TRANSIT, not APPROVED/SUBMITTED)

* Inventory Adjustments (InventoryAdjustmentController)
  - Endpoint: POST /api/v1/inventory-adjustments
    - Purpose: Initiates a draft inventory stock adjustment.
    - Required Permission: INVENTORY_ADJUSTMENT_CREATE
    - Request: InventoryAdjustmentRequest DTO (companyId, warehouseId, storeId, adjustmentType, items)
    - Response: ApiResponse<InventoryAdjustmentResponse>
    - Errors: 400 (validation, both locations specified, mismatched company)
  - Endpoint: PUT /api/v1/inventory-adjustments/{id}
    - Purpose: Updates a draft inventory adjustment.
    - Required Permission: INVENTORY_ADJUSTMENT_UPDATE
    - Request: InventoryAdjustmentUpdateRequest DTO
    - Response: ApiResponse<InventoryAdjustmentResponse>
    - Errors: 400 (not DRAFT)
  - Endpoint: POST /api/v1/inventory-adjustments/{id}/submit
    - Purpose: Submits an adjustment. Auto-approves to APPROVED if all line quantities are positive; otherwise transitions to SUBMITTED.
    - Required Permission: INVENTORY_ADJUSTMENT_CREATE
    - Errors: 400 (not DRAFT)
  - Endpoint: POST /api/v1/inventory-adjustments/{id}/approve
    - Purpose: Approves a submitted adjustment.
    - Required Permission: INVENTORY_ADJUSTMENT_APPROVE
    - Errors: 400 (not SUBMITTED)
  - Endpoint: POST /api/v1/inventory-adjustments/{id}/post
    - Purpose: Posts an approved adjustment. Performs physical stock updates in inventory_stock under PESSIMISTIC_WRITE locks, creates StockMovements, and fires an refresh event.
    - Required Permission: INVENTORY_ADJUSTMENT_POST
    - Errors: 400 (not APPROVED, negative adjustment exceeds on-hand stock)
  - Endpoint: POST /api/v1/inventory-adjustments/{id}/cancel
    - Purpose: Cancels a draft, submitted, or approved adjustment.
    - Required Permission: INVENTORY_ADJUSTMENT_CANCEL
    - Request Parameter: reason (String)
    - Errors: 400 (already posted or cancelled)

* Stock Counts (StockCountController)
  - Endpoint: POST /api/v1/stock-counts
    - Purpose: Creates a draft stock count session.
    - Required Permission: STOCK_COUNT_CREATE
    - Request: StockCountRequest DTO (companyId, warehouseId, storeId, countType, blindCount, approvalThresholdPercentage, clientReferenceId, remarks, cycleProductIds)
    - Response: ApiResponse<StockCountResponse>
    - Errors: 400 (validation, both locations, overlapping active count session)
  - Endpoint: PUT /api/v1/stock-counts/{id}
    - Purpose: Updates a draft stock count session.
    - Required Permission: STOCK_COUNT_UPDATE
    - Request: StockCountUpdateRequest DTO
    - Response: ApiResponse<StockCountResponse>
    - Errors: 400 (not DRAFT)
  - Endpoint: POST /api/v1/stock-counts/{id}/assign
    - Purpose: Assigns count to a user.
    - Required Permission: STOCK_COUNT_ASSIGN
    - Request Parameter: userId (Long)
    - Errors: 400 (not DRAFT/ASSIGNED)
  - Endpoint: POST /api/v1/stock-counts/{id}/start
    - Purpose: Starts counting, taking immutable snapshot of stock.
    - Required Permission: STOCK_COUNT_UPDATE
    - Errors: 400 (not ASSIGNED)
  - Endpoint: POST /api/v1/stock-counts/{id}/submit
    - Purpose: Submits counted quantities. If variance <= threshold, auto-approves.
    - Required Permission: STOCK_COUNT_SUBMIT
    - Request: StockCountSubmitRequest DTO
    - Errors: 400 (not IN_PROGRESS, uncounted items)
  - Endpoint: POST /api/v1/stock-counts/{id}/reject
    - Purpose: Rejects count.
    - Required Permission: STOCK_COUNT_APPROVE
    - Request Parameter: reason (String)
    - Errors: 400 (not SUBMITTED)
  - Endpoint: POST /api/v1/stock-counts/{id}/reopen
    - Purpose: Reopens rejected count, incrementing recount_number and clearing count quantities.
    - Required Permission: STOCK_COUNT_APPROVE
    - Errors: 400 (not REJECTED)
  - Endpoint: POST /api/v1/stock-counts/{id}/approve
    - Purpose: Approves count, automatically creating a variance InventoryAdjustment.
    - Required Permission: STOCK_COUNT_APPROVE
    - Errors: 400 (not SUBMITTED)
  - Endpoint: POST /api/v1/stock-counts/{id}/post
    - Purpose: Posts count and its Linked InventoryAdjustment.
    - Required Permission: STOCK_COUNT_POST
    - Errors: 400 (not APPROVED)
  - Endpoint: POST /api/v1/stock-counts/{id}/close
    - Purpose: Administratively closes count session.
    - Required Permission: STOCK_COUNT_POST
    - Errors: 400 (not POSTED)

* Inventory Replenishment (ReplenishmentController)
  - Endpoint: POST /api/v1/replenishment/rules
    - Purpose: Creates a new replenishment rule.
    - Required Permission: REPLENISHMENT_RULE_CREATE
  - Endpoint: GET /api/v1/replenishment/rules
    - Purpose: Lists replenishment rules.
    - Required Permission: REPLENISHMENT_RULE_VIEW
  - Endpoint: GET /api/v1/replenishment/rules/{id}
    - Purpose: Retrieves a replenishment rule by ID.
    - Required Permission: REPLENISHMENT_RULE_VIEW
  - Endpoint: PUT /api/v1/replenishment/rules/{id}
    - Purpose: Updates an existing replenishment rule.
    - Required Permission: REPLENISHMENT_RULE_UPDATE
  - Endpoint: DELETE /api/v1/replenishment/rules/{id}
    - Purpose: Soft deletes / deactivates a replenishment rule.
    - Required Permission: REPLENISHMENT_RULE_DELETE
  - Endpoint: POST /api/v1/replenishment/evaluate
    - Purpose: Evaluates all active replenishment rules for a company.
    - Required Permission: REPLENISHMENT_SUGGESTION_VIEW
  - Endpoint: POST /api/v1/replenishment/rules/{id}/evaluate
    - Purpose: Evaluates a single replenishment rule.
    - Required Permission: REPLENISHMENT_SUGGESTION_VIEW
  - Endpoint: GET /api/v1/replenishment/suggestions
    - Purpose: Lists replenishment suggestions.
    - Required Permission: REPLENISHMENT_SUGGESTION_VIEW
  - Endpoint: GET /api/v1/replenishment/suggestions/{id}
    - Purpose: Retrieves a replenishment suggestion by ID.
    - Required Permission: REPLENISHMENT_SUGGESTION_VIEW
  - Endpoint: POST /api/v1/replenishment/suggestions/{id}/acknowledge
    - Purpose: Acknowledges an open replenishment suggestion.
    - Required Permission: REPLENISHMENT_SUGGESTION_ACKNOWLEDGE
  - Endpoint: POST /api/v1/replenishment/suggestions/{id}/cancel
    - Purpose: Cancels an open or acknowledged suggestion.
    - Required Permission: REPLENISHMENT_SUGGESTION_CANCEL
  - Endpoint: POST /api/v1/replenishment/suggestions/{id}/create-purchase-request
    - Purpose: Auto-generates a Purchase Request from a suggestion.
    - Required Permission: REPLENISHMENT_SUGGESTION_ORDER
  - Endpoint: POST /api/v1/replenishment/suggestions/{id}/create-transfer
    - Purpose: Auto-generates an Inventory Transfer from a suggestion (stores only).
    - Required Permission: REPLENISHMENT_SUGGESTION_ORDER

* Procurement Analytics (ProcurementAnalyticsController)
  - Endpoint: GET /api/v1/analytics/procurement/summary
  - Endpoint: GET /api/v1/analytics/procurement/suppliers
  - Endpoint: GET /api/v1/analytics/procurement/payables-aging
  - Endpoint: GET /api/v1/analytics/procurement/purchase-orders
  - Endpoint: GET /api/v1/analytics/procurement/invoice-matching
  - Purpose: Queries materialized views for KPIs, aging Payables, and three-way matches.
  - Required Permission: ANALYTICS_VIEW
  - Request Parameter: companyId (Long)
  - Response: ApiResponse containing DTO dashboards
  - Errors: 403 Forbidden (missing permission), 400 (invalid param)

* Inventory Traceability (InventoryTraceabilityController)
  - Endpoint: POST /api/v1/inventory-lots
    - Purpose: Registers a new manufacturing lot/batch.
    - Required Permission: INVENTORY_LOT_CREATE
  - Endpoint: GET /api/v1/inventory-lots
    - Purpose: Lists all lots/batches.
    - Required Permission: INVENTORY_LOT_VIEW
  - Endpoint: GET /api/v1/inventory-lots/{id}
    - Purpose: Gets single lot details.
    - Required Permission: INVENTORY_LOT_VIEW
  - Endpoint: GET /api/v1/inventory-serials
    - Purpose: Lists tracked serial numbers.
    - Required Permission: INVENTORY_SERIAL_VIEW
  - Endpoint: GET /api/v1/inventory-trace/{productId}
    - Purpose: Retrieves end-to-end trace genealogy events for a product.
    - Required Permission: INVENTORY_TRACE_VIEW
  - Endpoint: GET /api/v1/inventory-trace/lot/{lotNumber}
    - Purpose: Retrieves trace history filtering by lot.
    - Required Permission: INVENTORY_TRACE_VIEW
  - Endpoint: GET /api/v1/inventory-trace/serial/{serialNumber}
    - Purpose: Retrieves trace history filtering by serial number.
    - Required Permission: INVENTORY_TRACE_VIEW
  - Endpoint: POST /api/v1/inventory-recalls
    - Purpose: Declares a recall campaign, quarantine-locking lots and serials.
    - Required Permission: INVENTORY_RECALL_CREATE
  - Endpoint: GET /api/v1/inventory-recalls
    - Purpose: Lists recall campaigns.
    - Required Permission: INVENTORY_RECALL_VIEW

* Customer Management (CustomerController)
  - Endpoint: POST /api/v1/customers
    - Purpose: Creates a new customer scoped to a company. Code is generated automatically if not provided.
    - Required Permission: CUSTOMER_CREATE
    - Request: CustomerRequest DTO
    - Response: ApiResponse<CustomerResponse>
    - Errors: 400 (validation), 404 (not found), 409 (duplicate code/email)
  - Endpoint: GET /api/v1/customers/{id}
    - Purpose: Retrieves customer details by ID.
    - Required Permission: CUSTOMER_VIEW
    - Response: ApiResponse<CustomerResponse>
    - Errors: 404 (not found)
  - Endpoint: GET /api/v1/customers
    - Purpose: Dynamic paginated search filtering by company, type, status, pricing, and activeOnly status.
    - Required Permission: CUSTOMER_VIEW
    - Response: ApiResponse<PageResponse<CustomerResponse>>
  - Endpoint: PUT /api/v1/customers/{id}
    - Purpose: Updates customer details.
    - Required Permission: CUSTOMER_UPDATE
    - Request: CustomerRequest DTO
    - Response: ApiResponse<CustomerResponse>
    - Errors: 400 (soft-deleted / inactive customer update block), 404 (not found)
  - Endpoint: PATCH /api/v1/customers/{id}/activate
    - Purpose: Activates an inactive, suspended, or blocked customer.
    - Required Permission: CUSTOMER_ACTIVATE
    - Response: ApiResponse<CustomerResponse>
  - Endpoint: PATCH /api/v1/customers/{id}/deactivate
    - Purpose: Deactivates a customer. Sets status to INACTIVE.
    - Required Permission: CUSTOMER_DEACTIVATE
    - Response: ApiResponse<CustomerResponse>
  - Endpoint: DELETE /api/v1/customers/{id}
    - Purpose: Soft deletes a customer record by setting status to INACTIVE.
    - Required Permission: CUSTOMER_DELETE
    - Response: ApiResponse<Void>

* Sales Orders (SalesOrderController)
  - Endpoint: POST /api/v1/sales-orders
    - Purpose: Creates a new sales order in DRAFT status. Validates company boundaries, client-reference-based idempotency, and snapshots customer profile and addresses.
    - Required Permission: SALES_ORDER_CREATE
    - Request: SalesOrderRequest DTO
    - Response: ApiResponse<SalesOrderResponse>
    - Errors: 400 (validation, cross-company customer match, missing billing/shipping address), 404 (not found)
  - Endpoint: PUT /api/v1/sales-orders/{id}
    - Purpose: Updates a draft sales order items and delivery dates.
    - Required Permission: SALES_ORDER_UPDATE
    - Request: SalesOrderRequest DTO
    - Response: ApiResponse<SalesOrderResponse>
    - Errors: 400 (not in DRAFT status), 404 (not found)
  - Endpoint: GET /api/v1/sales-orders/{id}
    - Purpose: Retrieves sales order details and its line items by ID.
    - Required Permission: SALES_ORDER_VIEW
    - Response: ApiResponse<SalesOrderResponse>
    - Errors: 404 (not found)
  - Endpoint: GET /api/v1/sales-orders
    - Purpose: Performs dynamic filter search and pagination for sales orders.
    - Required Permission: SALES_ORDER_VIEW
    - Request parameters: query, companyId, customerId, status, customerType, creditOverride, requestedDeliveryDateFrom, requestedDeliveryDateTo, createdBy, approvedBy
    - Response: ApiResponse<PageResponse<SalesOrderResponse>>
  - Endpoint: POST /api/v1/sales-orders/{id}/submit
    - Purpose: Submits a DRAFT order for approval. Transitions status to SUBMITTED and records submitter audit details.
    - Required Permission: SALES_ORDER_SUBMIT
    - Response: ApiResponse<SalesOrderResponse>
    - Errors: 400 (not DRAFT), 404 (not found)
  - Endpoint: POST /api/v1/sales-orders/{id}/approve
    - Purpose: Approves a SUBMITTED order, performing credit validation. If exceeded, requires SALES_ORDER_OVERRIDE_CREDIT_LIMIT, SALES_OVERRIDE_CREDIT_LIMIT, or CUSTOMER_OVERRIDE_CREDIT_LIMIT permission. Updates the customer's outstanding balance.
    - Required Permission: SALES_ORDER_APPROVE
    - Response: ApiResponse<SalesOrderResponse>
    - Errors: 400 (not SUBMITTED, inactive customer, credit check failure without permission), 404 (not found)
  - Endpoint: POST /api/v1/sales-orders/{id}/cancel
    - Purpose: Cancels a DRAFT, SUBMITTED, or APPROVED order. Reverts the customer's outstanding balance if the order was APPROVED.
    - Required Permission: SALES_ORDER_CANCEL
    - Request: SalesOrderCancelRequest DTO (reason)
    - Response: ApiResponse<SalesOrderResponse>
    - Errors: 400 (not in DRAFT, SUBMITTED, or APPROVED status), 404 (not found)

* Order Fulfillment & Picking (PickListController)
  - Endpoint: POST /api/v1/pick-lists
    - Purpose: Creates a DRAFT pick list for an approved sales order at a specific warehouse or store. Supports idempotency via client_reference_id. Validates XOR location (exactly one of warehouse or store), company ownership, and one-active-pick-list-per-location constraint.
    - Required Permission: PICK_LIST_CREATE
    - Request: PickListRequest DTO (companyId, salesOrderId, warehouseId, storeId, clientReferenceId)
    - Response: ApiResponse<PickListResponse> (HTTP 201)
    - Errors: 400 (inactive company, invalid SO status, both/neither location, active PL exists), 404 (not found)

  - Endpoint: POST /api/v1/pick-lists/{id}/release
    - Purpose: Releases a DRAFT pick list. Allocates stock using pessimistic write locks, creates ACTIVE inventory allocations, reserves stock, updates SalesOrderItem.allocatedQuantity, and logs SALES_RESERVE stock movements. Supports partial allocation when insufficient stock.
    - Required Permission: PICK_LIST_RELEASE
    - Response: ApiResponse<PickListResponse>
    - Errors: 400 (not DRAFT), 404 (not found)

  - Endpoint: POST /api/v1/pick-lists/{id}/start-picking
    - Purpose: Transitions a RELEASED pick list to PICKING status.
    - Required Permission: PICK_LIST_PICK
    - Response: ApiResponse<PickListResponse>
    - Errors: 400 (not RELEASED), 404 (not found)

  - Endpoint: POST /api/v1/pick-lists/{id}/complete-picking
    - Purpose: Records picked quantities for each item. Validates that picked quantity does not exceed allocated quantity.
    - Required Permission: PICK_LIST_PICK
    - Request: CompletePickingRequest DTO (items[productId, pickedQuantity])
    - Response: ApiResponse<PickListResponse>
    - Errors: 400 (not PICKING, picked > allocated), 404 (not found)

  - Endpoint: POST /api/v1/pick-lists/{id}/pack
    - Purpose: Transitions a PICKED pick list to PACKED status.
    - Required Permission: PICK_LIST_PACK
    - Response: ApiResponse<PickListResponse>
    - Errors: 400 (not PICKED), 404 (not found)

  - Endpoint: POST /api/v1/pick-lists/{id}/ship
    - Purpose: Ships a PACKED pick list. Deducts physical stock, clears reservations, updates SalesOrderItem.fulfilledQuantity, progresses SalesOrder status (PARTIALLY_FULFILLED or FULFILLED), creates CONSUMED/RELEASED allocation records, and logs SALES_SHIPMENT/SALES_RELEASE stock movements.
    - Required Permission: PICK_LIST_SHIP
    - Request: ShipRequest DTO (items[productId, shippedQuantity])
    - Response: ApiResponse<PickListResponse>
    - Errors: 400 (not PACKED, shipped > picked), 404 (not found)

  - Endpoint: POST /api/v1/pick-lists/{id}/cancel
    - Purpose: Cancels a pick list (any status except SHIPPED/CANCELLED). Rolls back stock reservations, restores SalesOrderItem.allocatedQuantity, marks allocations as CANCELLED, and logs SALES_RELEASE stock movements.
    - Required Permission: PICK_LIST_CANCEL
    - Request: SalesOrderCancelRequest DTO (reason)
    - Response: ApiResponse<PickListResponse>
    - Errors: 400 (already SHIPPED or CANCELLED), 404 (not found)

  - Endpoint: GET /api/v1/pick-lists/{id}
    - Purpose: Retrieves a single pick list by ID.
    - Required Permission: PICK_LIST_VIEW
    - Response: ApiResponse<PickListResponse>

  - Endpoint: GET /api/v1/pick-lists/sales-order/{salesOrderId}
    - Purpose: Lists all pick lists for a sales order.
    - Required Permission: PICK_LIST_VIEW

  - Endpoint: GET /api/v1/pick-lists/warehouse/{warehouseId}
    - Purpose: Lists all pick lists for a warehouse.
    - Required Permission: PICK_LIST_VIEW

  - Endpoint: GET /api/v1/pick-lists/store/{storeId}
    - Purpose: Lists all pick lists for a store.
    - Required Permission: PICK_LIST_VIEW

  - Endpoint: GET /api/v1/pick-lists/status/{status}
    - Purpose: Lists all pick lists with a given status.
    - Required Permission: PICK_LIST_VIEW

* Customer Invoices (CustomerInvoiceController)
  - Endpoint: POST /api/v1/customer-invoices
    - Purpose: Creates a new customer invoice in DRAFT status. Supports idempotency via client_reference_id.
    - Required Permission: CUSTOMER_INVOICE_CREATE
    - Request: CustomerInvoiceRequest DTO
    - Response: ApiResponse<CustomerInvoiceResponse> (HTTP 201)
  - Endpoint: PUT /api/v1/customer-invoices/{id}
    - Purpose: Updates an existing DRAFT customer invoice.
    - Required Permission: CUSTOMER_INVOICE_UPDATE
    - Request: CustomerInvoiceUpdateRequest DTO
    - Response: ApiResponse<CustomerInvoiceResponse>
  - Endpoint: GET /api/v1/customer-invoices/{id}
    - Purpose: Retrieves a single customer invoice by ID.
    - Required Permission: CUSTOMER_INVOICE_VIEW
    - Response: ApiResponse<CustomerInvoiceResponse>
  - Endpoint: GET /api/v1/customer-invoices
    - Purpose: Dynamic filter search and pagination for customer invoices.
    - Required Permission: CUSTOMER_INVOICE_VIEW
    - Request Parameters: companyId, customerId, salesOrderId, invoiceNumber, status, invoiceDateFrom, invoiceDateTo
    - Response: ApiResponse<PageResponse<CustomerInvoiceResponse>>
  - Endpoint: POST /api/v1/customer-invoices/{id}/submit
    - Purpose: Submits a DRAFT customer invoice. Locks edits and records submitter details.
    - Required Permission: CUSTOMER_INVOICE_SUBMIT
    - Response: ApiResponse<CustomerInvoiceResponse>
  - Endpoint: POST /api/v1/customer-invoices/{id}/approve
    - Purpose: Approves a SUBMITTED invoice. Generates a balanced Journal Entry, posts to ledger accounts (1400, 2200, 4000), updates customer outstanding balance (if ad-hoc), and triggers analytics refresh.
    - Required Permission: CUSTOMER_INVOICE_APPROVE
    - Response: ApiResponse<CustomerInvoiceResponse>
  - Endpoint: POST /api/v1/customer-invoices/{id}/cancel
    - Purpose: Cancels an APPROVED customer invoice. Reverses the ledger posting via a reversal Journal Entry, restores outstanding balances, and updates quantity records.
    - Required Permission: CUSTOMER_INVOICE_CANCEL
    - Request: CustomerInvoiceCancelRequest DTO (reason)
    - Response: ApiResponse<CustomerInvoiceResponse>
  - Endpoint: POST /api/v1/customer-invoices/{id}/void
    - Purpose: Voids a DRAFT or SUBMITTED invoice (accounting-safe cancellation before posting).
    - Required Permission: CUSTOMER_INVOICE_VOID
    - Response: ApiResponse<CustomerInvoiceResponse>

--------------------------------------------------------------------------------
6. WORKFLOW DOCUMENTATION
--------------------------------------------------------------------------------
This section defines business states, transitions, validations, and side effects.

* Purchase Requisition Workflow:
  - DRAFT: Initial creation. Editable.
  - SUBMITTED: Locks edits. Submitted for manager approval.
  - APPROVED: Manager validates items and signs off. Snapshots approved quantities.
  - CONVERTED_TO_PO: Purchase Request converted into a PO draft, linking entities.
  - REJECTED/CANCELLED: Terminal rejection states. Rejection reasons are audited.

* Purchase Order Workflow:
  - DRAFT: Initial PO.
  - ISSUED: PO approved. Expected delivery date must be present and future.
  - PARTIALLY_RECEIVED: Goods receipts contain quantities less than ordered.
  - RECEIVED: Goods receipt fully matched. Remaining quantity is 0.
  - CLOSED/CANCELLED: Terminal states.

* Goods Receipt Process:
  - Validations:
    - Linked PO must be ISSUED or PARTIALLY_RECEIVED.
    - Product must exist in the PO lines.
    - Received quantity cannot exceed remaining quantity on the PO line.
  - Side Effects:
    - Completing increments product physical on-hand quantity in inventory_stock.
    - Generates a GOODS_RECEIPT type StockMovement entry.
    - Decrements PO line remaining_quantity, increments received_quantity.
    - Cancelling a Goods Receipt reverses the entire stock addition and PO counts.

* Inventory Transfer Workflow:
  - DRAFT -> SUBMITTED -> APPROVED -> IN_TRANSIT -> RECEIVED -> CLOSED
  - Validation: Same-company constraint, store-to-warehouse transfer block.
  - Side Effects:
    - Approve: Verifies available stock (on_hand - reserved >= requested). Updates
      reserved_quantity on source stock. Publishes TRANSFER_RESERVE StockMovement.
    - Dispatch: Decrements physical quantity and reserved_quantity from source.
      Publishes TRANSFER_OUT StockMovement.
    - Receive: Increments physical quantity at destination. Marks received_quantity.
      Publishes TRANSFER_IN StockMovement. Transitions immediately to CLOSED.
    - Cancel: Reverses reserved_quantity on source. Publishes TRANSFER_RELEASE.

* Inventory Adjustment Workflow:
  - DRAFT -> SUBMITTED (if reductions exist) -> APPROVED -> POSTED
  - DRAFT -> APPROVED (auto-approved if additions only) -> POSTED
  - Validation: XOR location, mismatched company, negative stock check.
  - Side Effects:
    - Post: Locks stock and adjusts physical quantity by the delta. Publishes INVENTORY_ADJUSTMENT StockMovement. Fires InventoryAdjustmentRefreshEvent.

* Stock Count & Cycle Count Workflow:
  - DRAFT -> ASSIGNED -> IN_PROGRESS -> SUBMITTED -> APPROVED -> POSTED -> CLOSED
  - Exception Path: SUBMITTED -> REJECTED -> IN_PROGRESS (via reopen)
  - Validation: XOR location, mismatched company, overlapping active count check.
  - Blind Count Rule: Hide system quantity and variance details from DTO responses while active (DRAFT, ASSIGNED, IN_PROGRESS).
  - Side Effects:
    - Start: Snapshots current stock levels and takes immutable quantities.
    - Submit: Computes variances. Auto-approves directly to APPROVED if all lines are within threshold.
    - Reopen: Resets counted quantity and variance. Increments recount_number.
    - Approve: Automatically creates STOCK_COUNT_VARIANCE InventoryAdjustment.
    - Post: Triggers posting on the variance InventoryAdjustment.

* Inventory Replenishment Workflow:
  - Rules: DRAFT -> ACTIVE
  - Suggestions: OPEN -> ACKNOWLEDGED -> ORDERED -> FULFILLED
  - Suggestions alternate: OPEN/ACKNOWLEDGED -> CANCELLED
  - Validation:
    - Rule location must target exactly one warehouse or store.
    - Rule quantities constraints: min >= 0, point >= min, max > point, reorder > 0, reorder <= max.
    - Locations and parent rules must belong to same company.
    - Only one active (OPEN/ACKNOWLEDGED) suggestion per rule.
  - Side Effects:
    - Evaluation: Compares available quantity against reorder point. Creates OPEN suggestion if at or below reorder point.
    - Create PR: Auto-creates a draft Purchase Request using first active company supplier and links it. Transitions to ORDERED.
    - Create Transfer: Only for stores. Auto-creates a draft Inventory Transfer using first active company warehouse as source and links it. Transitions to ORDERED.

* Inventory Traceability & Recall Workflows:
  - FEFO Allocation: Lots are queried sorted by expiry_date ASC, optimizing allocation to limit waste.
  - Serial Status Lifecycle: State transitions are strictly validated:
    - IN_STOCK -> TRANSIT / SOLD / RECALLED / QUARANTINED (Valid)
    - TRANSIT / QUARANTINED -> IN_STOCK (Valid)
    - SOLD / RECALLED are terminal statuses and cannot transition further.
  - Automated Expiry Scheduler: A nightly background job runs at 01:15 AM, checks expired lots (expiry_date < today), updates status to EXPIRED, and records auto-expiry events.
  - Recall Quarantine: Creating a recall isolates the target lot/serial, updates statuses to RECALLED, and generates audit trail events. Unique indices block overlapping recalls.

* Customer Management Workflow:
  - Customer status: ACTIVE, INACTIVE, ON_HOLD, BLOCKED.
  - CRUD operations are restricted to company scope (no cross-company manipulation).
  - Validation: checks on credit_limit >= 0, outstanding_balance >= 0, discount_rate between 0 and 100, and payment_terms_days >= 0.
  - Customer Code Generation: Generated automatically on creation using format CUST-YYYY-xxxxxx.
  - Soft Delete: soft deletes are performed by updating status to INACTIVE.

* Sales Order Workflow:
  - Workflow States: DRAFT -> SUBMITTED -> APPROVED -> PARTIALLY_FULFILLED -> FULFILLED (via fulfillment module). CANCELLED is reachable from DRAFT, SUBMITTED, or APPROVED.
  - Snapshot Rule: Copies customer code, name, customer type, pricing tier, discount rate, tax profile, payment terms, billing/shipping addresses, and currency code at creation, ensuring historical data is preserved even if the customer profile is updated later.
  - Company Isolation: Enforces company validation (sales_order.company_id == customer.company_id), rejecting cross-company assignments.
  - Monetary Validations: Database constraints prevent negative amounts for subtotal, discount_amount, tax_amount, total_amount, outstanding_amount, and line item unit_price/quantity.
  - Credit Limit Verification: Upon approval, if outstanding_balance + total_amount exceeds the customer's credit_limit, the system blocks transition unless overridden by a user with SALES_ORDER_OVERRIDE_CREDIT_LIMIT, SALES_OVERRIDE_CREDIT_LIMIT, or CUSTOMER_OVERRIDE_CREDIT_LIMIT permissions. If overridden, credit_override flag is set to true.
  - Outstanding Balance Management: Approving a sales order increments the customer's outstanding_balance by the order's total_amount. Cancelling an approved sales order decrements the customer's outstanding_balance by the order's total_amount.
  - Idempotency support: client_reference_id (UUID) UNIQUE constraint prevents duplicate sales orders from API retries.
  - Inventory Integration: Approved orders feed into the fulfillment module (V25.3) for stock allocation and shipping.

* Order Fulfillment & Picking Workflow:
  - Workflow States: DRAFT -> RELEASED -> PICKING -> PICKED -> PACKED -> SHIPPED. CANCELLED is reachable from any active status (not SHIPPED/CANCELLED).
  - Idempotency: client_reference_id (UUID) per company prevents duplicate pick list creation from API retries.
  - Location Constraint: Exactly one of warehouse_id or store_id must be specified (XOR).
  - One Active Pick List Rule: Only one non-terminal (not SHIPPED/CANCELLED) pick list per sales order per location is allowed.
  - Side Effects:
    - Create (DRAFT): Populates pick list items from unallocated SalesOrderItem quantities.
    - Release (DRAFT -> RELEASED): Pessimistic write locks on InventoryStock. Allocates up to available stock (partial allocation supported). Creates ACTIVE InventoryAllocation records. Reserves stock (increments reserved_quantity). Updates SalesOrderItem.allocatedQuantity. Logs SALES_RESERVE StockMovement.
    - Start Picking (RELEASED -> PICKING): Status transition only.
    - Complete Picking (PICKING -> PICKED): Records picked quantities per item. Validates picked <= allocated.
    - Pack (PICKED -> PACKED): Status transition only.
    - Ship (PACKED -> SHIPPED): Pessimistic write locks on InventoryStock. Deducts shippedQuantity from physical stock. Clears full allocatedQuantity from reserved stock. Updates SalesOrderItem (fulfilledQuantity += shipped, allocatedQuantity -= allocated). Creates CONSUMED allocation for shipped quantity. Creates RELEASED allocation for remainder (partial shipment). Logs SALES_SHIPMENT and SALES_RELEASE StockMovements. Progresses SalesOrder to PARTIALLY_FULFILLED or FULFILLED.
    - Cancel (any -> CANCELLED): Reverses stock reservations. Restores SalesOrderItem.allocatedQuantity. Marks all ACTIVE allocations as CANCELLED. Logs SALES_RELEASE StockMovement.

* Customer Invoicing Workflow:
  - Workflow States: DRAFT -> SUBMITTED -> APPROVED -> PAID / PARTIALLY_PAID. CANCELLED is reachable from APPROVED. VOID is reachable from DRAFT or SUBMITTED.
  - Remaining-to-Invoice Validation: For order-based invoices, the sum of invoiced quantity across all invoices cannot exceed the sales order's fulfilled quantity: `quantity <= (fulfilled_quantity - invoiced_quantity)`.
  - Date Validation: Enforces `due_date >= invoice_date` at both database and service levels.
  - Accounts Verification: Checks for the existence of required general ledger accounts: Accounts Receivable (1400), Tax Payable (2200), and Revenue (4000) prior to invoice approval.
  - Side Effects:
    - Approve (SUBMITTED -> APPROVED): Automatically generates and posts a balanced double-entry Journal Entry (Debit 1400 Receivables, Credit 4000 Revenue, Credit 2200 Tax). For ad-hoc invoices, increments the customer's `outstanding_balance`.
    - Payment Allocation: Allocating a receivable payment decreases the invoice's `outstanding_balance` and transitions its status to `PARTIALLY_PAID` or `PAID`. For ad-hoc invoices, decreases the customer's `outstanding_balance` accordingly.
    - Cancel (APPROVED -> CANCELLED): Reverses the ledger entry by posting a balanced reversal Journal Entry, and decrements the customer's outstanding balance (if ad-hoc).
    - Void (DRAFT/SUBMITTED -> VOID): Marks the invoice as voided before posting, preventing any ledger impact.

--------------------------------------------------------------------------------
7. SECURITY DOCUMENTATION
--------------------------------------------------------------------------------
* Authentication Flow:
  Stateless token-based authentication. Client calls /api/v1/auth/login with
  credentials. Upon successful DaoAuthentication validation, AuthController signs
  a JWT using Spring Security OAuth2 (Jose library). Subsequent requests pass
  "Authorization: Bearer <token>".
* Authorization Model:
  Method security enabled via @EnableMethodSecurity. REST controller endpoints
  assert authority constraints using @PreAuthorize("hasAuthority('...')").
* Predefined System Roles:
  - ULTIMATE_ADMIN (Full system control)
  - REGIONAL_MANAGER (Scope restricted to regions)
  - WAREHOUSE_MANAGER (Logistics controls)
  - FINANCE_MANAGER (Ledger and payment controls)
  - PROCUREMENT_MANAGER (Supplier and requisition controls)

--------------------------------------------------------------------------------
8. EVENT-DRIVEN ARCHITECTURE
--------------------------------------------------------------------------------
Spring's application event publishing framework decouples business transactions
from analytical updates.

* Events:
  - ProcurementRefreshEvent: Fired on PO, GR, Invoice, or Payment changes.
  - InventoryRefreshEvent: Fired on Goods Receipt completions.
  - InventoryTransferRefreshEvent: Fired on inventory transfer approvals, dispatches, receipts, or cancellations.
  - InventoryAdjustmentRefreshEvent: Fired on posting inventory adjustments and Stock Counts.
  - ReplenishmentRefreshEvent: Fired on replenishment suggestions and rule evaluations.
  - InventoryTraceabilityRefreshEvent: Fired on recall creations or lot expiry checks.
  - PickListRefreshEvent: Fired on pick list releases, shipments, and cancellations.
  - CustomerInvoiceRefreshEvent: Fired on customer invoice creations, submissions, approvals, cancellations, payments, and voids.
* Listeners:
  - ProcurementRefreshEventListener
  - InventoryRefreshEventListener
  - InventoryTransferRefreshEventListener
  - InventoryAdjustmentRefreshEventListener
  - ReplenishmentRefreshEventListener
  - InventoryTraceabilityRefreshEventListener
  - PickListRefreshEventListener
  - CustomerInvoiceRefreshEventListener
* Processing:
  Listeners use @Async and @TransactionalEventListener(phase = TransactionPhase.AFTER_COMMIT).
  They trigger AnalyticsRefreshService inside new propagation transactions to update
  corresponding PostgreSQL materialized views.

--------------------------------------------------------------------------------
9. ANALYTICS DOCUMENTATION
--------------------------------------------------------------------------------
* Materialized Views:
  - mv_procurement_summary: Company metrics.
  - mv_supplier_performance: On-time delivery rates and lead times.
  - mv_payables_aging: Groups unpaid invoices by aging buckets (current, 30, 60, 90+ days).
  - mv_po_fulfilment: Order lines fulfillment status.
  - mv_invoice_matching: Flags three-way matching discrepancies (unit price/quantity).
  - mv_inventory_kpis: Aggregates total stock value, unique product counts, out of stock, low stock, and overstocks.
  - mv_inventory_aging_expiry: Calculates stock aging buckets (0-30, 31-90, 91-180, 180+ days) and lot expiry risks.
  - mv_inventory_abc_xyz: Performs ABC/XYZ demand volatility classifications over 365 days.
  - mv_inventory_slow_dead: Identifies slow-moving and dead stock items.
  - mv_inventory_replenishment_metrics: Tracks rules coverage, cycle times, and stockout prevention rates.
  - mv_inventory_traceability_metrics: Audits recalls, serial counts, and product compromise rates.
  - mv_inventory_turnover: Calculates COGS, average stock value, and turnover ratios.
  - mv_sales_kpis: Aggregates total invoices, total invoiced, total paid, and total outstanding receivables.
  - mv_receivables_dashboard: Summarizes invoice counts, total amounts, and outstanding balances by status.
  - mv_customer_aging: Dynamically computes customer aging buckets (current, 1-30, 31-60, 61-90, 90+ days) based on invoice due dates.
* Indexes for Concurrent Refreshes:
  Unique indexes (e.g. uidx_mv_payables_aging, uidx_mv_inventory_kpis) allow non-blocking concurrent refreshes
  ("REFRESH MATERIALIZED VIEW CONCURRENTLY view_name"). Expression indexes cannot be used for concurrent refreshes;
  thus, unique indices are created over plain columns (company_id, warehouse_id, store_id) directly.
* Scheduling:
  AnalyticsRefreshService runs scheduled staggered cron updates to minimize DB contention:
  - Every 15 mins (0, 15, 30, 45): KPIs
  - Every 15 mins (5, 20, 35, 50): Aging/Expiry
  - Every 15 mins (10, 25, 40, 55): ABC/XYZ
  - Every 15 mins (12, 27, 42, 57): Traceability
  - Every 15 mins (14, 29, 44, 59): Replenishment
  - Every 15 mins (7, 22, 37, 52): Turnover

--------------------------------------------------------------------------------
10. TESTING DOCUMENTATION
--------------------------------------------------------------------------------
* Strategy: MockMvc integration tests executing against real contexts.
* Database Isolation: Test classes annotated with @Transactional roll back changes.
* Setup Isolation: Setup methods (@BeforeEach) must call repository.flush() after deleting
  stale entries. This ensures pending Hibernate deletions are executed before database
  immediate inserts (IDENTITY generation strategy) fire, preventing unique constraint violations.

--------------------------------------------------------------------------------
11. DEPLOYMENT DOCUMENTATION
--------------------------------------------------------------------------------
* Build command:
  ./mvnw.cmd clean package -DskipTests
* Database:
  A PostgreSQL instance must be configured via application.properties / environment variables:
  - spring.datasource.url=jdbc:postgresql://<host>:<port>/plus33_erp
  - spring.datasource.username=<user>
  - spring.datasource.password=<password>

--------------------------------------------------------------------------------
12. TROUBLESHOOTING GUIDE
--------------------------------------------------------------------------------
* Optimistic Locking Failures (ObjectOptimisticLockingFailureException):
  Occur when two threads edit the same entity (version mismatch). Resolve by implementing
  optimistic retry wrappers or locking via PESSIMISTIC_WRITE.
* Unique Constraint Violations:
  Often due to missing @Transactional in integration tests or missing flush() after deletes in @BeforeEach.
  Resolve by verifying transactional rollback and setup flush.
* Hibernate Flush Ordering:
  When using clear() + addAll() on collection replacements, Hibernate executes inserts before deletes.
  Declaring composite uniqueness constraints as DEFERRABLE INITIALLY DEFERRED is required.

--------------------------------------------------------------------------------
13. DEVELOPER GUIDELINES
--------------------------------------------------------------------------------
* Collection Update Constraint Rule:
  Whenever a collection mapping uses cascade = CascadeType.ALL and orphanRemoval = true,
  and the collection is replaced using clear() + addAll(newItems), the composite unique
  key containing the parent foreign key in the child table MUST be declared as
  "DEFERRABLE INITIALLY DEFERRED". This defers unique constraint evaluation until commit,
  preventing statement-level violations during Hibernate's insert-before-delete flush.

--------------------------------------------------------------------------------
14. CUSTOMER RETURNS & CREDIT NOTES (V25.5)
--------------------------------------------------------------------------------
This module completes the Sales lifecycle by supporting customer returns, product inspections, restocking/scrapping/quarantining, credit note generation, double-entry ledger reversals, and Accounts Receivable updates.

* Database Tables:
  - customer_returns: Header for return requests. Tracks company, customer, sales order, invoice, warehouse, store, return number, client reference ID, status, reason, remarks, and audit fields (createdBy, approvedBy, receivedBy, inspectedBy, closedBy, cancelledBy).
  - customer_return_items: Line items. Tracks sales order item, invoice item, product, quantity, inspection result (RESTOCK, SCRAP, QUARANTINE), inspection notes, lot, and serial.
  - credit_notes: Header for credit notes. Tracks company, customer, return, invoice, credit note number, client reference ID, status (DRAFT, APPROVED, POSTED, CANCELLED), subtotal, tax, discount, total, journal entry, and audit fields.
  - credit_note_items: Line items under a credit note.

* Domain Entities:
  - CustomerReturn (com.plus33.erp.sales.entity.CustomerReturn)
  - CustomerReturnItem (com.plus33.erp.sales.entity.CustomerReturnItem)
  - CreditNote (com.plus33.erp.sales.entity.CreditNote)
  - CreditNoteItem (com.plus33.erp.sales.entity.CreditNoteItem)

* REST APIs:
  - POST /api/v1/customer-returns (Create request)
  - PUT /api/v1/customer-returns/{id} (Update request - DRAFT only)
  - GET /api/v1/customer-returns/{id} (Retrieve single return)
  - GET /api/v1/customer-returns (Search and page returns)
  - POST /api/v1/customer-returns/{id}/approve (Approve request)
  - POST /api/v1/customer-returns/{id}/receive (Receive returned goods)
  - POST /api/v1/customer-returns/{id}/inspect (Record line-level inspection outcomes)
  - POST /api/v1/customer-returns/{id}/close (Close return, process inventory, generate & post credit note)
  - POST /api/v1/customer-returns/{id}/cancel (Cancel return)
  - GET /api/v1/credit-notes/{id} (Retrieve single credit note)
  - GET /api/v1/credit-notes (Search credit notes)

* Workflows & Side Effects:
  - Return requested -> Approved -> Received -> Inspected -> Closed (or Cancelled).
  - Validation: Partial return checks prevent over-returning (returnedQuantity <= invoicedQuantity). Only products on the original invoice may be returned. Serials and lots must exist, belong to the invoice, and not be already returned.
  - Close Return (INSPECTED -> CLOSED):
    - RESTOCK: Increments physical stock, registers CUSTOMER_RETURN stock movement, records trace event, and sets serial status to IN_STOCK.
    - SCRAP: Registers CUSTOMER_RETURN stock movement (zero stock change), sets serial to RECALLED, and records trace event.
    - QUARANTINE: Sets serial/lot status to QUARANTINED, registers stock movement, and records trace event.
    - Generates Credit Note (POSTED) and posts balanced Journal Entry (Debit 4000 Revenue, Debit 2200 Tax, Credit 1400 AR).
    - Updates CustomerInvoice creditedAmount, remainingBalance, and transitions status (PARTIALLY_CREDITED / FULLY_CREDITED).
    - Deducts credit note total from Customer outstandingBalance.
  - Analytics Refresh: Publishes CustomerReturnRefreshEvent on APPROVAL, RECEIVE, INSPECT, and CLOSE, triggering asynchronous refresh of materialized views (mv_sales_kpis, mv_receivables_dashboard, mv_customer_aging, mv_inventory_kpis, mv_inventory_traceability_metrics).

* Verification Notes:
  - Status: PASSED and VERIFIED.
  - Integration Test Suite: com.plus33.erp.sales.controller.CustomerReturnControllerIntegrationTest
  - Execution Command: `.\mvnw.cmd clean test -Dtest=CustomerReturnControllerIntegrationTest`
  - Test Scope: Covers the entire end-to-end return request lifecycle (Draft -> Approved -> Received -> Inspected -> Closed) and verifies:
    - Company-scoped multi-tenant isolation and security permission checks.
    - Over-return validation: blocks returns exceeding remaining invoiced quantity.
    - Product invoice membership check: returns 409 Conflict if a returned product does not belong to the invoice.
    - Serial/lot validation: checks status (must be SOLD) and prevents duplicate returns.
    - Granular line-level inspection outcomes (RESTOCK, SCRAP, QUARANTINE) with stock movements, trace events, and status updates.
    - Credit Note generation (POSTED) and customer balance reduction.
    - Balanced GL reversing Journal Entry posting: Debit 4000 (Sales Returns), Debit 2200 (Tax Payable), Credit 1400 (Accounts Receivable).
    - Customer Invoice status transition (PARTIALLY_CREDITED / FULLY_CREDITED) and remaining balance calculations.
    - Event-driven dashboard updates (CustomerReturnRefreshEvent).

--------------------------------------------------------------------------------
15. ACCOUNTS RECEIVABLE (V25.6)
--------------------------------------------------------------------------------
This module extends the Sales and invoicing architecture by introducing dedicated Accounts Receivable reporting, customer balance summaries, chronological customer ledger statements, overdue invoice tracking, and bad-debt write-off processing with automated General Ledger postings.

* Database Tables:
  - ar_write_offs: Records each bad-debt write-off as an immutable accounting event. Tracks company, customer, customer invoice, write-off amount, write-off date, reason, journal entry, and audit fields (createdBy, writtenAt).

* Domain Entities:
  - ARWriteOff (com.plus33.erp.ar.entity.ARWriteOff)

* REST APIs:
  - GET /api/v1/ar/aging (Get AR Aging Report from mv_customer_aging view)
  - GET /api/v1/ar/summary (Get AR Summary / Dashboard metrics)
  - GET /api/v1/ar/customers/{customerId}/balance (Get Customer AR Balance)
  - GET /api/v1/ar/customers/{customerId}/statement (Get Customer AR Statement ledger)
  - GET /api/v1/ar/overdue (Get paginated list of overdue invoices)
  - POST /api/v1/ar/write-offs (Record an AR write-off)
  - GET /api/v1/ar/write-offs/{id} (Get write-off details by ID)
  - GET /api/v1/ar/write-offs (Search write-offs with filtering)

* Workflows & Side Effects:
  - Customer Statement: Dynamically builds a chronological ledger of customer invoices, payments/allocations, credit notes, and write-offs within a date range, calculating a running balance in-memory.
  - Overdue Invoices: Dynamically lists approved/partially paid invoices where due_date < current_date and outstanding_balance > 0.
  - Write-off Processing:
    - Verifies invoice status is eligible (APPROVED, PARTIALLY_PAID, PARTIALLY_CREDITED).
    - Verifies write-off amount does not exceed invoice outstanding balance.
    - Generates unique write-off reference numbers using sequence `ar_write_off_seq`.
    - Automatically posts balanced General Ledger journal entry: Debit Bad Debt Expense (5300) / Credit Accounts Receivable (1400).
    - Decreases both CustomerInvoice.outstandingBalance and Customer.outstandingBalance.
    - Does NOT change the invoice status (unless fully written off, where outstanding balance is reduced to zero and write-off serves as the accounting event).
    - Publishes CustomerInvoiceRefreshEvent to trigger asynchronous refresh of materialized views (mv_sales_kpis, mv_receivables_dashboard, mv_customer_aging).

* Verification Notes:
  - Status: PASSED and VERIFIED.
  - Integration Test Suite: com.plus33.erp.ar.controller.ARControllerIntegrationTest
  - Execution Command: `.\mvnw.cmd clean test -Dtest=ARControllerIntegrationTest`
  - Test Scope: Covers the entire Accounts Receivable reporting and write-off lifecycle:
    - Customer balance retrieval, overdue invoice listing, and statement generation.
    - Recording a write-off and asserting write-off persistence.
    - Verification of automatic balanced GL Journal Entry generation (Debit 5300, Credit 1400) linked to the write-off.
    - Verification of customer invoice outstanding balance and customer outstanding balance reductions.
    - Chronological statement listing with correct running balance calculations.

--------------------------------------------------------------------------------
16. ACCOUNTS PAYABLE (V26)
--------------------------------------------------------------------------------
This module introduces a unified Accounts Payable (AP) management and reporting layer over the existing Procurement and Finance systems. It provides real-time dashboard KPIs, configurable aging bucketing, supplier outstanding balances, chronological statements, overdue bill tracking, and secure voiding transactions.

* Database Tables & Enhancements:
  - Altered status check constraint on supplier_invoices to support SUBMITTED and VOID statuses (V66).
  - Seeded system permissions: AP_VIEW, AP_STATEMENT_VIEW, and VENDOR_BILL_VOID mapped to ULTIMATE_ADMIN (V67).

* REST APIs:
  - GET /api/v1/ap/dashboard (Get AP Dashboard summary and KPIs)
  - GET /api/v1/ap/aging (Get AP Aging Report with dynamic, configurable intervals)
  - GET /api/v1/ap/suppliers/{supplierId}/balance (Get Supplier AP Balance)
  - GET /api/v1/ap/suppliers/{supplierId}/statement (Get Supplier Chronological Statement ledger)
  - GET /api/v1/ap/overdue (Get paginated list of overdue supplier invoices)
  - GET /api/v1/ap/analytics (Get detailed AP analytics and cash requirement forecasts)
  - POST /api/v1/supplier-invoices/{id}/submit (Submit a draft invoice, transitioning DRAFT -> SUBMITTED)
  - POST /api/v1/supplier-invoices/{id}/void (Void a draft or submitted invoice, transitioning DRAFT/SUBMITTED -> VOID)

* Workflows & Side Effects:
  - Lifecycle Rules: Supplier invoices transition through DRAFT -> SUBMITTED -> APPROVED -> PARTIALLY_PAID -> PAID.
  - Void Rules: Voiding is allowed only on DRAFT and SUBMITTED statuses. Approved, partially paid, or paid invoices are blocked from voiding and must be reversed using the accounting cancel/reversal process.
  - Supplier Statement: Dynamically builds a chronological ledger of bills (credit), payments/allocations (debit), and cancellations (debit) within a date range, starting from an opening balance (sum of all historical movements before the starting date) and computing a running balance in-memory.
  - Configurable Aging: Dynamically maps all outstanding supplier invoices into configurable day intervals (e.g. 15, 30, 60, 120) or defaults to standard buckets (30, 60, 90) in Java, avoiding hardcoded database aging scripts.
  - Company Isolation: Enforces multi-company validation on all endpoints, ensuring that all supplier, invoice, and payment requests belong to the same company boundary.
  - Event-driven refreshes: Publishes SupplierInvoiceRefreshEvent on all status changes to asynchronously refresh payables and procurement views (mv_payables_aging, mv_procurement_summary, mv_supplier_performance, mv_po_fulfilment, mv_invoice_matching) via Spring Event listeners.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suite: com.plus33.erp.ap.controller.APControllerIntegrationTest
  - Execution Command: .\mvnw.cmd clean test -Dtest=APControllerIntegrationTest
  - Test Scope: Covers the entire Accounts Payable reporting and lifecycle:
    - Dashboard KPIs: asserts correct calculation of payables, current due, overdue, due this week, open bills, average payment days, awaiting approvals, and cash forecasts.
    - Configurable aging: asserts correct bucketing of outstanding bills with custom intervals.
    - Chronological statements: asserts correct opening, running, and closing balances with bill/payment/cancellation flows.
    - Overdue bill paginated listing.
    - Void rules: asserts that draft and submitted invoices can be voided, while approved invoices are blocked.
    - Multi-tenant company isolation and security permission checks.

--------------------------------------------------------------------------------
17. SUPPLIER PAYMENTS & PAYMENT RUNS (V26.2)
--------------------------------------------------------------------------------
This module automates bulk, consolidated batch payments to suppliers. It provides invoice selection and reservation, supplier-level payment consolidation, transaction-isolated execution (ensuring partial batch success), rich auditing of execution results, strategy-pattern-based bank file exports stored securely on the local filesystem, and an operational dashboard.

* Database Tables:
  - payment_runs: Header table representing the batch run.
    - Fields: id (BIGSERIAL PK), company_id (BIGINT FK -> companies), run_number (VARCHAR(50) UNIQUE), status (VARCHAR(50)), payment_method (VARCHAR(50)), payment_date (DATE), client_reference_id (UUID UNIQUE), total_amount (DECIMAL), successful_payments_count (INTEGER), failed_payments_count (INTEGER), export_file_name (VARCHAR(255)), export_storage_path (VARCHAR(255)), export_checksum (VARCHAR(64)), export_generated_at (TIMESTAMP), created_by / approved_by / executed_by / cancelled_by (VARCHAR(100)), created_at / approved_at / executed_at / cancelled_at (TIMESTAMP), error_message (TEXT).
  - payment_run_invoices: Details table linking selected invoices to the payment run.
    - Fields: id (BIGSERIAL PK), payment_run_id (BIGINT FK -> payment_runs), supplier_invoice_id (BIGINT FK -> supplier_invoices), payment_amount (DECIMAL).
  - payment_run_supplier_results: Child auditing table tracking execution results per supplier.
    - Fields: id (BIGSERIAL PK), payment_run_id (BIGINT FK -> payment_runs), supplier_id (BIGINT FK -> suppliers), payment_id (BIGINT FK -> payments, nullable), status (VARCHAR(50)), amount (DECIMAL), started_at (TIMESTAMP), completed_at (TIMESTAMP), error_message (TEXT).

* Security Permissions (Mapped to ULTIMATE_ADMIN):
  - PAYMENT_RUN_CREATE: Allows creating and calculating a payment run.
  - PAYMENT_RUN_VIEW: Allows viewing payment run details, listings, dashboards, and downloading bank exports.
  - PAYMENT_RUN_APPROVE: Allows approving a calculated payment run.
  - PAYMENT_RUN_EXECUTE: Allows executing an approved payment run.

* REST APIs:
  - POST /api/v1/payment-runs (Create and calculate a payment run. Filters by company, currency, due date, supplier, and payment method. Reserves matching invoices by setting supplier_invoices.payment_run_id, locking them from other runs.)
  - GET /api/v1/payment-runs/{id} (Retrieve details of a single payment run, including invoice lines and supplier execution results.)
  - GET /api/v1/payment-runs (Search and page payment runs with company-level tenant isolation.)
  - POST /api/v1/payment-runs/{id}/approve (Approve a calculated DRAFT run, transitioning status DRAFT -> APPROVED.)
  - POST /api/v1/payment-runs/{id}/execute (Execute payments for an APPROVED run. Integrates unique client_reference_id header/body to enforce idempotency.)
  - POST /api/v1/payment-runs/{id}/cancel (Cancel a DRAFT or APPROVED run, releasing all reserved invoices and clearing the reservation foreign keys.)
  - GET /api/v1/payment-runs/dashboard (Fetch operational KPIs: total payment runs, total processed amount, successful vs failed payments, and pending runs.)
  - GET /api/v1/payment-runs/{id}/download-export (Download the generated bank export file from filesystem.)

* Workflows & Architectural Safeguards:
  - **Company Scopes**: Strictly validates that run.company_id == invoice.company_id == supplier.company_id.
  - **Invoice Reservation**: Calculated runs lock invoices via a foreign key `supplier_invoices.payment_run_id`. These invoices cannot be selected in other runs or paid individually until the run is executed or cancelled.
  - **Payment Consolidation**: During execution, the run groups outstanding invoices by supplier. It generates exactly one consolidated `Payment` (DEBIT) and multiple `PaymentAllocation` records per supplier.
  - **Transaction Partitioning**: To ensure partial batch success, each supplier's payment execution is wrapped in an independent transaction (`Propagation.REQUIRES_NEW`) using a dedicated `PaymentRunExecutionHelper`. If a supplier payment fails (e.g. invalid bank details or GL account configuration), the failure is recorded in `payment_run_supplier_results` and committed. The main run transaction continues to the next supplier, preventing a single failure from failing the entire batch.
  - **Scalable Export Storage**: The system supports CSV, ISO 20022 XML, SEPA XML, and NEFT/RTGS text formats via the Strategy Pattern (`BankExportGenerator`). Instead of storing massive file payloads in the database, `FileStorageService` writes them to the local directory `storage/exports/bank`. The database persists only metadata (`export_file_name`, `export_storage_path`, `export_checksum`, `export_generated_at`), keeping the DB light and high-performing.
  - **Idempotency Safeguard**: The execution requires a unique `client_reference_id` UUID. If a client retries the execution of an already completed or processing run, the API returns the existing record immediately without re-executing.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suite: com.plus33.erp.paymentrun.controller.PaymentRunControllerIntegrationTest
  - Execution Command: `.\mvnw.cmd clean test -Dtest=PaymentRunControllerIntegrationTest`
  - Test Scope: Comprises 7 comprehensive test scenarios:
    1. Calculation and invoice selection/reservation.
    2. Supplier payment consolidation and allocation.
    3. Multi-tenant company isolation.
    4. Execution idempotency (prevent double payments).
    5. Isolated transaction partitioning (partial success / isolated rollbacks).
    6. Filesystem bank export writing and checksum validation.
    7. Operational dashboard KPIs.

--------------------------------------------------------------------------------
18. FINANCIAL REPORTING (V27.1)
--------------------------------------------------------------------------------
This module implements robust, mathematically validated financial statements (Trial Balance, Income Statement, and Balance Sheet) leveraging the double-entry General Ledger system, with configurable fiscal calendars, multi-currency functional conversion, period lock controls, and audited fiscal closing/reopening workflows.

* Database Tables:
  - fiscal_years: Tracks company fiscal years, statuses, and links opening/closing journals.
    - Fields: id (BIGSERIAL PK), company_id (BIGINT FK -> companies), fiscal_year (INTEGER), start_date (DATE), end_date (DATE), status (VARCHAR(30)), opening_journal_id (BIGINT FK -> journal_entries), closing_journal_id (BIGINT FK -> journal_entries), closed_by (VARCHAR(100)), closed_at (TIMESTAMP).
    - Constraints: UNIQUE (company_id, fiscal_year), CHECK (status IN ('OPEN', 'CLOSED')).
  - period_locks: Tracks the locked period date and type per company.
    - Fields: id (BIGSERIAL PK), company_id (BIGINT FK -> companies UNIQUE), lock_date (DATE), lock_type (VARCHAR(30)), locked_by (VARCHAR(100)), locked_at (TIMESTAMP), reason (VARCHAR(255)).
    - Constraints: CHECK (lock_type IN ('SOFT', 'HARD')).
  - period_lock_overrides: Audit log tracking soft-lock overrides.
    - Fields: id (BIGSERIAL PK), company_id (BIGINT FK -> companies), user_email (VARCHAR(100)), override_at (TIMESTAMP), original_lock_date (DATE), transaction_date (DATE), journal_entry_id (BIGINT FK -> journal_entries), reason (VARCHAR(255)).
  - exchange_rates: Multi-rate database-backed currency conversion rates.
    - Fields: id (BIGSERIAL PK), company_id (BIGINT FK -> companies), from_currency (VARCHAR(3)), to_currency (VARCHAR(3)), rate_type (VARCHAR(30)), rate (DECIMAL(12,6)), effective_date (DATE).
    - Constraints: UNIQUE (company_id, from_currency, to_currency, rate_type, effective_date), CHECK (rate > 0), CHECK (from_currency <> to_currency), CHECK (rate_type IN ('SPOT', 'CORPORATE', 'MONTH_END', 'AVERAGE')).

* Domain Entities & Enums:
  - Enums: FiscalYearStatus (OPEN, CLOSED), PeriodLockType (SOFT, HARD), ExchangeRateType (SPOT, CORPORATE, MONTH_END, AVERAGE), ClosingEntryType (NORMAL, ADJUSTMENT, REOPEN_REVERSAL).
  - Entities: FiscalYear, PeriodLock, PeriodLockOverride, ExchangeRate under package com.plus33.erp.finance.reporting.entity.

* REST APIs:
  - GET /api/v1/financial-reports/trial-balance (Generate Trial Balance statement)
  - GET /api/v1/financial-reports/income-statement (Generate Income Statement/P&L)
  - GET /api/v1/financial-reports/balance-sheet (Generate Balance Sheet as of date)
  - GET /api/v1/financial-reports/export (Export Trial Balance, Income Statement, or Balance Sheet to CSV or HTML)
  - POST /api/v1/financial-reports/period-lock (Create or update a period lock)
  - GET /api/v1/financial-reports/period-lock (Retrieve current period lock status)
  - POST /api/v1/financial-reports/fiscal-year-close (Perform audited year-end closing)
  - POST /api/v1/financial-reports/fiscal-year-reopen/{fiscalYear} (Reopen a closed fiscal year)

* Workflows & Side Effects:
  - **Fiscal Calendar Validation**: Dynamically validates company fiscal month/day settings using leap-year-safe LocalDate construction. Invalid combinations (e.g. February 31) are caught in the service layer, returning 400 Bad Request.
  - **Functional Currency Conversion**: In functional mode, converts foreign currency ledger entries to Functional Currency using exchange rate lookups matching company, date, from/to currencies, and rate type.
  - **Same-Currency Bypass**: Immediately returns rate = 1.0 without database queries if from_currency == to_currency.
  - **Spring AOP PeriodLockAspect**: Intercepts service-layer boundaries and repository journal saves, validating transaction dates against company locks.
  - **Lock Override Auditing**: Soft lock violations by authorized users (Finance Manager / Ultimate Admin) are permitted but write an immutable override log in a separate transaction (Propagation.REQUIRES_NEW), surviving business rollbacks. Hard locks strictly reject postings.
  - **Double-Counting Protection**: Balance Sheet aggregates Retained Earnings correctly by dynamically computing net income only for the current unclosed year and combining it with account 3100 (historical closed years). Caches this calculation in-memory.
  - **Immutable Closing Entries**: Standard reversals or modifications to journals marked as closing entries are strictly blocked.
  - **Pre-Close Completeness Check**: Rejects fiscal year closing if any unposted (DRAFT) journal entries exist in the period.
  - **Audited Non-Destructive Reopening**: Reopening a closed fiscal year is protected by chronological checks (blocks if subsequent year is closed) and posts an Automatic Reversing Journal (marked with closing_type = 'REOPEN_REVERSAL') to preserve a complete audit trail.
  - **Decoupled Report Exporters**: Supports native CSV and print-optimized HTML exports incorporating comprehensive metadata headers (Report Name, Company, Date, Generated By, Generated At, Currency, Exchange Rate Type).

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suite: com.plus33.erp.finance.reporting.controller.FinancialReportingControllerIntegrationTest
  - Execution Command: `.\mvnw.cmd clean test -Dtest=FinancialReportingControllerIntegrationTest`
  - Test Scope: Comprises 10 compliance and accounting scenarios:
    1. Fiscal calendar date validations (invalid combos rejected).
    2. Exchange rates & functional currency reporting (conversions and same-currency bypasses).
    3. Service-layer soft/hard locks & override auditing (rollback survival).
    - Export authorization checks (FINANCIAL_REPORT_EXPORT permission).
    - CSV and HTML exports with metadata headers.

-------------------------------------------------------------------------------19. FIXED ASSET MANAGEMENT (V27.2 - ULTIMATE ENTERPRISE EDITION)
--------------------------------------------------------------------------------
This module implements an industry-leading, highly scalable Fixed Asset Management system capable of handling portfolios of 10,000 to 500,000+ assets. It manages the complete fixed asset lifecycle—from CWIP construction, multi-stage approval transfers, and parent-child hierarchy recursive accounting to revaluations, impairments, leases, reservations, maintenance work orders, soft deletes, and compliance legal holds.

* Database Tables & Sequence:
  - fixed_asset_code_seq: Database sequence generating unique asset codes (AST-YYYY-xxxxxx).
  - asset_categories: Category metadata mapping GL accounts and capitalization thresholds.
  - fixed_assets: Main asset register tracking acquisition, components, location, documents, warranty, insurance, health scores, GIS, IoT telemetry, and functional currency values.
  - fixed_asset_books: Join table for multi-book (FINANCIAL, TAX, MANAGEMENT) and multi-frequency (MONTHLY, QUARTERLY, HALF_YEARLY, YEARLY) depreciation tracking.
  - fixed_asset_depreciation_logs: Logs individual asset depreciation entries.
  - fixed_asset_transfers: Logs physical location transfers with multi-stage approval workflows (REQUESTED, MANAGER_APPROVED, IN_TRANSIT, RECEIVED, ACTIVE).
  - fixed_asset_maintenances / fixed_asset_maintenance_plans: Scheduled preventive maintenance and logged maintenance events.
  - fixed_asset_work_orders: Formal maintenance work orders tracking technicians, labor/parts costs, and downtime.
  - fixed_asset_assignments: Logs employee and location assignment history.
  - fixed_asset_audits / fixed_asset_audit_items: Periodic physical audits automatically transitioning missing assets to LOST and damaged to UNDER_MAINTENANCE.
  - fixed_asset_revaluations / fixed_asset_impairments: IAS 16 revaluation reserves and IAS 36 impairment losses.
  - fixed_asset_leases: Operating and finance lease tracking with lease liability account mapping.
  - fixed_asset_relations: Tracks asset split, merge, replacement, and parent-child components relations.
  - fixed_asset_utilization: Usage-based utilization readings (hours, mileage, units).
  - fixed_asset_attachments / fixed_asset_attachment_versions: Multi-version document attachments.
  - fixed_asset_reservations: Shared asset checkouts and reservations.
  - fixed_asset_insurance_claims: Tracking claims history against policies.
  - fixed_asset_downtimes: Tracks lost operational hours and production revenue.
  - fixed_asset_notifications: Database-backed alerts for maintenance, warranties, insurance, and audits.
  - fixed_asset_fx_logs: Multi-currency revaluation logs.
  - fixed_asset_history: Immutable, chronological audit timeline tracking all actions.

* Security Permissions:
  - FIXED_ASSET_VIEW, FIXED_ASSET_CREATE, FIXED_ASSET_ACQUIRE, FIXED_ASSET_DEPRECIATE, FIXED_ASSET_TRANSFER, FIXED_ASSET_DISPOSE, FIXED_ASSET_MAINTAIN, FIXED_ASSET_ASSIGN, FIXED_ASSET_AUDIT.
  - FIXED_ASSET_REVALUE, FIXED_ASSET_IMPAIR, FIXED_ASSET_LEASE, FIXED_ASSET_SPLIT_MERGE, FIXED_ASSET_APPROVE_DISPOSAL, FIXED_ASSET_RESERVE, FIXED_ASSET_CLAIM, FIXED_ASSET_TRANSFER_APPROVE, FIXED_ASSET_DOWNTIME, FIXED_ASSET_BULK, FIXED_ASSET_WORK_ORDER, FIXED_ASSET_COMPLIANCE.

* REST APIs (Exposing 25+ Secure Endpoints):
  - POST /api/v1/fixed-assets/categories (Create category)
  - GET /api/v1/fixed-assets/categories (Get categories)
  - POST /api/v1/fixed-assets (Register draft asset)
  - GET /api/v1/fixed-assets/{id} (Get asset details)
  - GET /api/v1/fixed-assets (Search/page assets with company isolation)
  - POST /api/v1/fixed-assets/{id}/acquire (Capitalize/acquire asset and post GL)
  - POST /api/v1/fixed-assets/{id}/assign (Assign asset to employee/location)
  - POST /api/v1/fixed-assets/{id}/transfer (Request transfer)
  - POST /api/v1/fixed-assets/transfers/{transferId}/approve (Approve transfer)
  - POST /api/v1/fixed-assets/transfers/{transferId}/receive (Mark transfer received)
  - POST /api/v1/fixed-assets/{id}/maintain (Log maintenance)
  - POST /api/v1/fixed-assets/{id}/dispose (Dispose/sell asset and post GL)
  - POST /api/v1/fixed-assets/depreciate (Run batch/dry-run depreciation)
  - POST /api/v1/fixed-assets/audits (Submit physical audit)
  - GET /api/v1/fixed-assets/dashboard (Get standard dashboard KPIs)
  - POST /api/v1/fixed-assets/{id}/revalue (Revalue asset)
  - GET /api/v1/fixed-assets/{id}/revaluations (Get revaluations)
  - POST /api/v1/fixed-assets/{id}/impair (Impair asset)
  - GET /api/v1/fixed-assets/{id}/impairments (Get impairments)
  - POST /api/v1/fixed-assets/{id}/partial-dispose (Partially dispose asset)
  - POST /api/v1/fixed-assets/{id}/capitalize-cwip (Capitalize CWIP asset)
  - POST /api/v1/fixed-assets/{id}/leases (Lease asset)
  - GET /api/v1/fixed-assets/{id}/leases (Get leases)
  - POST /api/v1/fixed-assets/{id}/split (Split asset)
  - POST /api/v1/fixed-assets/merge (Merge assets)
  - POST /api/v1/fixed-assets/{id}/utilization (Log usage)
  - GET /api/v1/fixed-assets/{id}/utilization (Get usage history)
  - GET /api/v1/fixed-assets/{id}/tco (Get TCO analysis)
  - POST /api/v1/fixed-assets/maintenance-plans (Create maintenance plan)
  - GET /api/v1/fixed-assets/{id}/maintenance-plans (Get maintenance plans)
  - POST /api/v1/fixed-assets/reservations (Reserve asset)
  - GET /api/v1/fixed-assets/{id}/reservations (Get reservations)
  - POST /api/v1/fixed-assets/work-orders (Create work order)
  - POST /api/v1/fixed-assets/work-orders/{workOrderId}/complete (Complete work order)
  - GET /api/v1/fixed-assets/{id}/work-orders (Get work orders)
  - GET /api/v1/fixed-assets/{id}/history (Get history log)
  - POST /api/v1/fixed-assets/{id}/legal-hold (Toggle legal hold)
  - DELETE /api/v1/fixed-assets/{id} (Soft delete asset)
  - GET /api/v1/fixed-assets/{id}/rollup (Get hierarchy rollup KPIs)

* Key Workflows & Enterprise Safeguards:
  - **Recursive Roll-up Accounting**: Service recursively traverses nested parent-child assets to roll up acquisition costs, net book values, accumulated depreciation, maintenance costs, and average health scores.
  - **Multi-Currency Valuations**: Translates foreign acquisitions using historical exchange rates, recording FX gain/loss adjustments over time in `fixed_asset_fx_logs`.
  - **Capitalization Threshold Rules**: If `acquisitionCost < category.capitalizationThreshold`, automatically registers as `EXPENSED`, skips depreciation, and immediately expenses cost: `DR Category Depreciation Expense / CR Accounts Payable` (bypassing asset accounts).
  - **Construction Work-in-Progress (CWIP)**: Registers construction assets under `UNDER_CONSTRUCTION` status, accumulating costs in `cwip_account_id` and capitalizes via `DR Asset Account / CR CWIP Account`.
  - **Split & Merge Engine**: Distributes cost and accumulated depreciation proportionally on splits, and aggregates them on merges while maintaining relationships.
  - **TCO, Downtime & Health Scoring**: Dynamically calculates Total Cost of Ownership (TCO), cost per operating hour, and asset health scores based on age, maintenance logs, and downtime events.
  - **Legal Holds & Soft Deletes**: Supports soft deletes (hiding assets from standard queries) and legal holds (blocking any delete, disposal, split, or transfer operation).
  - **Scheduler sweeps**: Monthly depreciation run and daily sweeps for maintenance reminders, warranty expiries, insurance renewals, audit discrepancies, and data retention sweeps.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suite: com.plus33.erp.finance.assets.controller.FixedAssetControllerIntegrationTest
  - Execution Command: `.\mvnw.cmd clean test -Dtest=FixedAssetControllerIntegrationTest`
  - Test Scope: Expanded to 10 comprehensive test methods covering all 15 compliance and lifecycle scenarios.

--------------------------------------------------------------------------------
20. TREASURY & CASH MANAGEMENT (V29 - ENTERPRISE TMS EDITION)
--------------------------------------------------------------------------------
This module implements a treasury management system (TMS) at an enterprise tier, incorporating cash pooling, in-house banking, automated statement reconciliation, multi-currency revaluation, configurable risk policy limits, double-entry GL ledger mapping, and immutable event auditing.

* Database Tables & Sequence:
  - treasury_entities: Organization nodes (GROUP, REGIONAL, COUNTRY, COMPANY) for multi-tenant isolation.
  - treasury_accounting_profiles: Profile codes mapping bank accounts, fee accounts, FX gain/loss accounts, interest accounts, and investment accounts to Chart of Accounts (COA) IDs.
  - treasury_authorization_matrices: Maker/checker permissions specifying minimum and maximum cash flow approval limits.
  - bank_relationships / credit_facilities: Facility metadata tracking limits, types (REVOLVING, OVERDRAFT, TERM_LOAN), and expiry metrics.
  - treasury_risk_policies: Policy engine tracking PAYMENT, FX, INVESTMENT, and BANK limit rules with soft warning thresholds and HARD_BLOCK breach actions.
  - treasury_journal_entries: Immutable double-entry ledger database for cash transactions, FX trades, fees, and maturities.
  - treasury_audit_events: Chronological history logs of status transitions and approvals.
  - netting_pools / netting_transactions: Multilateral netting for corporate netting runs.
  - hedging_contracts / hedge_effectiveness_tests: IFRS 9 hedge valuation logs.

* Security Permissions:
  - TREASURY_VIEW, TREASURY_CONFIG_EVICT, TREASURY_POLICY_EVAL, TREASURY_JOURNAL_POST.

* REST APIs (Exposing Secure Endpoints):
  - GET /api/v2/treasury/journal/event/{type}/{id} (Get ledger entries)
  - POST /api/v2/treasury/journal/cash-transfer/{id} (Post cash transfer journal entry)
  - GET /api/v2/treasury/audit/{aggregateType}/{aggregateId} (Get audit trail)
  - GET /api/v2/treasury/audit/company/{companyId} (Get company-wide audit timeline)
  - GET /api/v2/treasury/policies/evaluate (Dry-run evaluate policy limits)
  - GET /api/v2/treasury/ai/forecast (AI advisory forecast)
  - GET /api/v2/treasury/ai/anomalies (AI anomaly detection alerts)
  - POST /api/v2/treasury/config/evict-cache/{companyId} (Evict configuration registry cache)

* Key Workflows & Enterprise Safeguards:
  - **Payment Lifecycle State Machine**: Implements a strict, immutable transition matrix across 15 states (DRAFT -> VALIDATED -> COMPLIANCE_PENDING -> COMPLIANCE_APPROVED -> APPROVAL_PENDING -> APPROVED -> QUEUED -> TRANSMITTING -> TRANSMITTED -> BANK_ACCEPTED -> PARTIALLY_SETTLED -> SETTLED -> RECONCILED) preventing illegal transition leaps.
  - **MoneyAmount Value Object**: Handles bank arithmetic using banker's rounding (HALF_EVEN) and enforces currency guards to prevent mismatched operations.
  - **Double-Entry Journal Postings**: Posts balanced Debits and Credits automatically per event type. Idempotency guarantees prevent duplicate ledger entries.
  - **Chronological Auditing**: Employs `Propagation.REQUIRES_NEW` to guarantee that transaction failures in calling systems do not roll back the generated audit log record.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suite: com.plus33.erp.finance.treasury.controller.TreasuryEnterpriseIntegrationTest
  - Execution Command: `.\mvnw test -Dtest=TreasuryEnterpriseIntegrationTest`
  - Test Scope: 35 integration tests verifying state transitions, rounding behavior, rule limits, audit timeline persistence, API controllers, and Treasury client SDK.

--------------------------------------------------------------------------------
21. ENTERPRISE TAX MANAGEMENT & COMPLIANCE (V30 - ULTIMATE ENTERPRISE EDITION)
--------------------------------------------------------------------------------
This module implements a centralized tax management and compliance engine, featuring dynamic date-versioned rates, multidimensional priority-based tax determination rules, tax posting profiles, automated invoice tax calculations (for sales, purchases, and GL posting), standalone tax adjustments, localized digital signing e-invoicing compliance (ZATCA, PEPPOL, IRP), and filing lifecycle tracking.

* Database Tables & Sequence:
  - tax_categories: Tax categories (e.g. VAT, GST, Sales Tax).
  - tax_posting_profiles: GL ledger account mappings (input tax, output tax, reverse charge, recoverable, etc.) per company and category.
  - tax_rates: Date-versioned rate percentages.
  - tax_groups / tax_group_lines: Grouping of multiple versioned tax rates.
  - tax_registrations: Multi-registration hierarchy (VAT, TRN, GST, TIN) scoped by company, supplier, and customer.
  - tax_exemption_certificates: Active exemption certificates zero-rating tax.
  - tax_determination_rules: Multidimensional priority-based lookup engine matching customer, supplier, product, place of supply, and fallback criteria.
  - tax_adjustment_entries: Standalone tax adjustment journals for posting manual corrections.
  - tax_override_requests: Approval workflow tracking manual tax amount overrides.
  - einvoice_compliance_logs: Audit trails of government portal compliance submissions.
  - tax_calculation_logs: Immutable operational audit logging recording JSON request payloads, resolved rules, applied rates, execution durations, and override flags.
  - tax_configuration_versions: Company-scoped configuration versions specifying active rules/rates snapshots.
  - compliance_queue_items: Background queuing engine for asynchronous e-invoicing submission.
  - tax_calendar / tax_filings: Filing calendar periods and generated tax returns.

* Materialized Views:
  - mv_input_tax: Materialized raw items input tax for purchases.
  - mv_output_tax: Materialized raw items output tax for sales.
  - mv_reverse_charge: Materialized purchase invoice reverse charge offsets.
  - mv_withholding_tax: Materialized withholding tax (WHT) records from treasury investments.
  - mv_tax_filings: Flat filing register combining calendar and submitted tax returns.
  - mv_tax_dashboard: Executive aggregation of total input, output, and net tax liability.
  - mv_monthly_tax_trend: Executive monthly trend grouping output tax.
  - mv_tax_by_jurisdiction: Tax grouping by geographical jurisdictions.
  - mv_tax_by_product_category: Tax grouping by product categories.
  - mv_tax_recovery_analysis: Analytical recovery comparison (recoverable vs non-recoverable).

* Security Permissions & Role Mapping:
  - TAX_VIEW: View tax configurations.
  - TAX_MANAGE: Manage tax properties and configurations.
  - TAX_OVERRIDE: Trigger manual tax overrides.
  - TAX_REPORT_EXPORT: Export tax audit registers.
  - TAX_EINVOICE: Force manual compliance submission.
  - TAX_FILING: Process and submit tax filings.
  - TAX_RATE_MANAGE / TAX_RULE_MANAGE / TAX_POSTING_PROFILE_MANAGE: Legacy specific administrative roles.
  - (Permissions are mapped automatically to ULTIMATE_ADMIN and TAX_MANAGER roles).

* Key Engine Abstractions & Services:
  - **TaxEngineRegistry**: Programmatic O(1) registry for resolving engine providers (VAT, GST, SALES_TAX) at runtime.
  - **VAT, GST, and Sales Tax Providers**:
    - `VatTaxEngineProvider`: Standard VAT calculations (inclusive/exclusive support).
    - `GstTaxEngineProvider`: India-style CGST/SGST (intra-state split) and IGST (inter-state) component separation.
    - `SalesTaxEngineProvider`: US-style destination-based sales tax (always tax-exclusive, non-recoverable for the seller).
  - **TaxCalculationEngine**: Coordinates configuration versions, manual overrides (Precedence 1 checks), and registry delegation. Records calculation logs, publishes Spring events (`TaxCalculatedEvent`, `TaxOverrideAppliedEvent`), and returns diagnostic metadata.
  - **TaxDeterminationRuleEngine**: Evaluates specificity sorting sequentially: Profile Match (8) > Product Category Match (4) > Place of Supply Match (2) > Incoterms Match (1) > Fallback (0), using Priority as tie-breaker.
  - **TaxConfigurationCache & TaxConfigurationCacheEvictAspect**: Concurrent caching layer memoizing active rates and determination rules, with automated AOP aspect eviction on configuration version changes.
  - **TaxMetricsRegistry & TaxDiagnosticsController**: Internal metrics engine capturing execution durations, cache hit/miss rates, and provider usage statistics exposed secured via `/api/v2/tax/diagnostics` (`TAX_VIEW`).
  - **TaxJournalService**: Centralized posting helper for CustomerInvoice and SupplierInvoice services, generating correct double-entries and capitalizing non-recoverable tax.
  - **ComplianceQueueWorker**: Asynchronous background processor utilizing retry policies with exponential backoff and dead-letter queue transitions.
  - **TaxFilingService**: Progresses periods through DRAFT -> CALCULATED -> SUBMITTED.
  - **EInvoiceComplianceService**: Decoupled digital signature provider mapping internal payloads into compliant digital structures (ZATCA, IRP, and PEPPOL).

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suite: com.plus33.erp.finance.tax.TaxManagementIntegrationTest
  - Execution Command: `.\mvnw clean test -Dtest=TaxManagementIntegrationTest`
  - Test Scope: 19 comprehensive integration scenarios validating configuration cache performance, automated aspect eviction, metrics diagnostics endpoints, registration, math, exemption, specificity sorting, GST components, US Sales Tax, overrides, logging, versioning, event publishing, and compliance worker retries/dead-letter.

--------------------------------------------------------------------------------
22. ENTERPRISE WORKFORCE MANAGEMENT & PAYROLL (V31 - ULTIMATE ENTERPRISE EDITION)
--------------------------------------------------------------------------------
This module implements a comprehensive multi-company, multi-jurisdiction workforce payroll engine supporting pluggable statutory tax calculation providers, double-entry GL journal postings, multi-tier approval workflows, audit timeline persistence, and event bus integrations.

* Key Engine Abstractions & Statutory Providers:
  - **PayrollEngineRegistry**: Programmatic O(1) registry for dynamically resolving country-specific engines (US, UAE, IND, DEU, GBR) at runtime.
  - **Regional Tax Engines**:
    - `UsPayrollEngine`: US Federal/State income tax withholding slabs, FICA Social Security (6.2%) and Medicare (1.45%).
    - `UaePayrollEngine`: UAE zero-tax WPS compliant files and End-of-Service Gratuity (EOSB) accrual math.
    - `IndiaPayrollEngine`: Indian Statutory Provident Fund (PF), ESIC, Professional Tax, and TDS tax slab deductions.
    - `GermanyPayrollEngine`: German Krankenversicherung (health), Rentenversicherung (pension), Pflegeversicherung (nursing care), and Solidarity Surcharge.
    - `UkPayrollEngine`: UK PAYE tax bands and National Insurance (NI) categories.

* Centralized Accounting & Event Bus Integrations:
  - **PayrollJournalServiceImpl**: Posts double-entry GL journal entries automatically on payroll processing (Debit Salary Expense 5300 / Credit Payroll Payable 2200).
  - **Event Bus & Auditing**: Publishes Spring Application Events (`PayrollCalculatedEvent`, `PayrollApprovedEvent`, `PayrollPaidEvent`, `PayrollReversedEvent`) and logs immutable chronological audit entries with `REQUIRES_NEW` propagation.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suite: com.plus33.erp.workforce.PayrollEnterpriseIntegrationTest
  - Execution Command: `.\mvnw test -Dtest=PayrollEnterpriseIntegrationTest`
  - Test Scope: 30 comprehensive integration test scenarios covering multi-company isolation, multi-calendar execution, statutory withholdings, GL postings, EOSB accruals, retro-adjustments, Treasury payment runs, reversals, ESS self-service statement downloads, audit logging, and event bus publishing.

--------------------------------------------------------------------------------
23. ENTERPRISE MANUFACTURING, MES & COST ACCOUNTING (V32 - ENTERPRISE EDITION)
--------------------------------------------------------------------------------
This module implements a complete, enterprise-grade manufacturing suite (MRP II / MES / Cost Accounting) supporting engineering revisions, manufacturing calendars with shifts and exceptions, finite capacity-constrained planning, material and labor tracking confirmations, quality inspections, and standard vs actual cost variance GL settlements.

* Database Tables:
  - bom_headers / bom_lines: Bills of Materials tracking components, quantities, and scrap factors.
  - routing_headers / routing_operations: Routing steps specifying sequence, work centers, setup/run times, and scheduling mode.
  - work_centers: Work centers managing hourly labor/overhead rates and capacity.
  - manufacturing_calendars / calendar_shifts / calendar_exceptions: Operational calendars tracking standard capacity shifts and downtime.
  - mrp_runs / planned_orders / mrp_pegging_links: Material Requirement Planning runs generating suggestions and tracking demand/supply links.
  - production_orders / production_operations / production_confirmations: Manufacturing orders tracking status, material/labor consumption, scrap, and rework.
  - quality_inspections / quality_inspection_steps: Quality gates tracking inspection items, parameters, actual values, and approvals.
  - manufacturing_costs / manufacturing_cost_details: Detailed cost cards tracking standard vs actual material, labor, and overhead components.
  - manufacturing_events / manufacturing_event_details: Domain event auditing tracking status transitions, material genealogy, and anomalies.

* Security Permissions:
  - MANUFACTURING_VIEW: Allows viewing BOMs, routings, calendars, work centers, and order records.
  - MANUFACTURING_MANAGE: Allows managing/creating BOMs, routings, calendars, and work centers.
  - PRODUCTION_RELEASE: Allows releasing planned/draft manufacturing orders and allocating components.
  - PRODUCTION_EXECUTE: Allows executing confirmations, scraps, rework, and shop floor entries.
  - QUALITY_APPROVE: Allows inspecting and approving QA sheets and completing production orders.

* Key Engine Abstractions & Services:
  - **Engineering Change Management (ECM/ECO)**: Manages BOM and routing revisions through formal approval workflows, tracking effective dates.
  - **Manufacturing Order State Machine**: Implements a strict lifecycle (DRAFT -> PLANNED -> RELEASED -> MATERIAL_ALLOCATED -> IN_PROGRESS -> PARTIALLY_COMPLETED -> COMPLETED -> QUALITY_PENDING -> CLOSED plus exception states HOLD, CANCELLED, SCRAPPED, REWORK) preventing illegal state leaps.
  - **Capacity Planning & Finite Scheduling**: Dynamically schedules operations backwards or forwards, adjusting dates based on work center shifts and calendar exceptions.
  - **MES Shop Floor Execution**: Logs production confirmations, scraps, and rework while tracing full component-to-finished-good material genealogy.
  - **Quality Inspection & Approvals**: Formal QA checkpoints before production completion, preventing the closure of defective orders.
  - **Cost Accounting & Variance Settlements**: Real-time overhead absorption, material costs, labor costs, and variance postings (e.g. Price, Efficiency, Usage variances) to GL accounts (DR WIP 1400 / CR Variance 5500).

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suite: com.plus33.erp.manufacturing.ManufacturingIntegrationTest
  - Execution Command: `.\mvnw.cmd test -Dtest=ManufacturingIntegrationTest`
  - Test Scope: 50+ compliance and integration validation scenarios verifying BOM/routing release, finite scheduling, MRP suggestion explosions, QA inspection workflows, shop floor confirmations (scrap/rework/genealogy), and manufacturing cost variance ledger settlements.

--------------------------------------------------------------------------------
24. ENTERPRISE SUPPLY CHAIN & ADVANCED WAREHOUSE MANAGEMENT (V33 - ENTERPRISE EDITION)
--------------------------------------------------------------------------------
This module implements an enterprise-grade warehouse, logistics, and supply chain management suite (WMS/TMS) supporting multi-warehouse zone/bin location topology, strategy-based put-away and picking, multi-source inventory reservations, an immutable double-entry movement ledger, cycle counting with blind audit modes, and pluggable 3PL carrier integration.

* Database Tables:
  - warehouse_zones / warehouse_locations / location_stock: Bin-level warehouse layout tracking capacity, velocity classes, pick sequences, and real-time inventory stock with optimistic locking.
  - advance_shipping_notices / asn_lines / put_away_work: Inbound receipt management tracking ASN statuses, gate arrivals, dock doors, and directed put-away tasks.
  - waves / picking_work / shipments / shipment_lines: Outbound fulfillment management tracking wave creation, strategy-allocated pick lists, packing, and carrier dispatch.
  - yard_locations / dock_doors / dock_schedules / truck_checkins / appointment_slots: Yard and dock management tracking vehicle entry, loading bay scheduling, and turnaround times.
  - cross_dock_orders / cross_dock_lines: Cross-dock management routing inbound receipts directly to outbound shipping lanes.
  - warehouse_tasks: Centralized task engine tracking picking, put-away, cycle count, replenishment, and transfer task lifecycles.
  - inventory_reservations / inventory_allocation_logs: Multi-source reservation tracking demand reservations across sales orders, work orders, transfers, and projects.
  - inventory_movements: Immutable double-entry inventory ledger recording movement types, locations, quantities, unit costs, total costs, and idempotency keys.
  - cycle_count_plans / cycle_count_tasks / cycle_count_results: Inventory audit tracking scheduling, blind count task generation, variance calculation, auto-approval thresholds, and GL journal adjustments.
  - replenishment_plans / replenishment_tasks: Automated pick-face replenishment tracking min/max thresholds, Kanban rules, and task generation.
  - carriers: Carrier master registry tracking 3PL integration providers, API endpoints, and credential vault references.

* Security Permissions:
  - WMS_VIEW: Allows viewing warehouse layouts, stock levels, ASNs, waves, cycle counts, and carrier records.
  - WMS_MANAGE: Allows configuring zones, locations, count plans, replenishment rules, and carrier profiles.
  - WMS_OPERATIONS: Allows executing inbound gate check-in, put-away, wave picking, pick confirmations, and shipment dispatch.
  - WMS_ANALYTICS: Allows accessing warehouse performance metrics and refreshing analytics views.

* Key Engine Abstractions & Services:
  - **WarehouseStrategyRegistry**: O(1) strategy resolver for pluggable put-away algorithms (NEAREST_EMPTY_BIN, FIXED_BIN, CAPACITY_BASED, ZONE_BASED, ABC_CLASS, VELOCITY_BASED) and picking allocation algorithms (FEFO, FIFO, LIFO, BATCH, CLUSTER, ZONE, SERIAL_CONTROLLED).
  - **Immutable Double-Entry Inventory Ledger**: Records tamper-proof movements with unique idempotency keys across 13 movement types, serving as the single source of truth for stock history.
  - **Pessimistic & Optimistic Concurrency Control**: Uses pessimistic WRITE locks during bin updates and JPA @Version optimistic locking on picking work to guarantee high-throughput concurrency safety.
  - **CarrierRegistry (TMS Abstraction)**: Standardized 3PL carrier interface (FEDEX, DHL, UPS, LOCAL, CUSTOM) for freight rate estimation, shipment booking, tracking, and proof-of-delivery.
  - **Cycle Count & GL Posting Engine**: Supports blind count auditing, configurable auto-approval variance thresholds, supervisor approvals, and automatic GL journal postings for variance adjustments.
  - **Asynchronous WmsEventBus**: Dispatches typed domain events (AsnReceivedEvent, PickingCompletedEvent, ShipmentDispatchedEvent, etc.) to safely refresh materialized views inside isolated REQUIRES_NEW transactions.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suite: com.plus33.erp.wms.WarehouseManagementIntegrationTest
  - Execution Command: `.\mvnw.cmd test -Dtest=WarehouseManagementIntegrationTest`
  - Test Scope: 50 enterprise scenarios verifying warehouse layout topology, strategy registry resolution, carrier provider operations, ASN check-in and put-away, double-entry movement ledger recording, stock reservations, wave picking execution, blind cycle counting, and replenishment rules.

--------------------------------------------------------------------------------
25. ENTERPRISE CUSTOMER RELATIONSHIP MANAGEMENT & OMNICHANNEL COMMERCE (V34)
--------------------------------------------------------------------------------
This module implements a comprehensive, enterprise-grade CRM, Sales Force Automation, and Omnichannel Commerce platform. It provides Lead Management, Opportunity pipelines, Quote versioning and approval workflows, sales territories, timeline tracking, commission settlement lifecycles, and a registry for omnichannel connectors.

* Database Tables:
  - crm_leads: Unqualified prospects and campaign attributions.
  - crm_opportunities: Qualified sales pipeline deals with stage tracking.
  - crm_quotes / crm_quote_versions / crm_quote_version_lines: Versions, subtotal, discounts, tax, and quote approval locking.
  - crm_territories: Geographic regions, rep assignments, effective ranges, and versioning.
  - crm_timeline_events: Unified chronological customer communications and ERP touchpoints.
  - crm_commissions: Earned rep payouts tracking calculation, approval, and posting.
  - crm_event_store: Audit events tracking schema, causation, and correlation attributes.
  - crm_cases: Support tickets, categories, SLA breaches, and resolution details.

* Key Abstractions & Services:
  - **Customer360RefreshService**: Aggregates master customer profiles, active pipelines, AR balances, tickets, CLTV, and churn indexes into CQRS projections.
  - **LeadConversionSaga**: Coordinates Customer → Credit Profile → Tax Profile → Territory Assignment → Customer360 Projection → Opportunity → Activity Timeline, supporting rollback compensation.
  - **OpportunityForecastEngine**: Calculations for Pipeline, Weighted, and Commit forecasts.
  - **PricingCalculationService**: Centralized pricing, promotion, and discount engine.
  - **SalesWorkflowEngine**: Manages Quote/Discount workflow approval policies.
  - **TerritoryAssignmentEngine**: Geographic territory allocation routing based on regions and segments.
  - **CommissionEngine**: Tiered, split, and team rate payouts calculator.
  - **ChannelRegistry (Omnichannel TMS)**: Pluggable registry supporting Shopify, Amazon, WhatsApp, and Custom providers.
  - **AiPredictionRegistry**: Programmatic predictions (LeadScoringProvider, ChurnProvider, ForecastProvider).

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suite: com.plus33.erp.crm.CrmEnterpriseIntegrationTest
  - Execution Command: `.\mvnw.cmd surefire:test -Dtest=CrmEnterpriseIntegrationTest`
  - Test Scope: 140+ comprehensive scenarios verifying lead lifecycle, opportunity stage machine, quote versions, forecast engines, geo territory routing, timeline event logs, and omnichannel provider resolution.

--------------------------------------------------------------------------------
26. ENTERPRISE SERVICE MANAGEMENT & FIELD SERVICE OPERATIONS (V35)
--------------------------------------------------------------------------------
This module implements a comprehensive ESM and Field Service Operations suite, managing support ticket lifecycles, SLA breach monitoring, work order task execution, workforce skill dispatch scheduling, mobile van stocks, preventive maintenance schedules, IoT triggers, and time-and-material or contract renewals service billing.

* Database Tables:
  - esm_work_orders / esm_work_order_tasks / esm_checklists: Rich lifecycle state machines for work orders and checklists.
  - esm_installed_assets / esm_warranties / esm_service_contracts: Installed asset registry, warranties, and AMC agreements.
  - esm_technician_sessions: Technician logins, GPS status, offline mobility queues.
  - esm_van_stocks / esm_parts_reservations: Mobile spare parts van inventories and reservations.
  - esm_pm_plans / esm_iot_alarms: Preventive plans and critical IoT alarms.
  - esm_billing_records: Service billing lifecycle tracking.
  - esm_event_store: Sourced event database logs.
  - esm_surveys: CSAT, NPS, and CES customer satisfaction reports.

* Key Abstractions & Services:
  - **SlaEngine**: Priority-based resolution limits and automated breach warnings.
  - **DispatchScheduler**: Rule-based technician scheduling matching skills, availability, and territories.
  - **VanInventoryService**: Mobile van warehouse control and parts ledger consumption logs.
  - **PreventiveMaintenanceEngine**: Automated generation of calendar or runtime-hour work orders.
  - **IotAlarmProcessor**: Filters duplicates and auto-dispatches technicians on critical device failures.
  - **ServiceBillingService**: Life cycle state billing calculations and postings to GL entries.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suite: com.plus33.erp.esm.EsmEnterpriseIntegrationTest
  - Execution Command: `.\mvnw.cmd surefire:test -Dtest=EsmEnterpriseIntegrationTest`
  - Test Scope: 150+ comprehensive scenarios verifying support SLAs, technician scheduling, warranties, AMC contracts, van spare parts, IoT alarms, and survey submissions.

--------------------------------------------------------------------------------
27. ENTERPRISE PROCUREMENT, STRATEGIC SOURCING & SRM (V36)
--------------------------------------------------------------------------------
This module implements a comprehensive sourcing, vendor management, and procurement operations suite (SRM), enforcing requisitions lifecycle state machine, RFQ versions, scoring and bids awards, Approved Vendor List (AVL) risk level metrics, blanket pricing contract amendment versionings, spending policies, and 3-way/4-way matching constraints.

* Database Tables:
  - procurement_requisitions: Advanced purchase requests tracking state transitions.
  - procurement_rfqs / procurement_rfq_versions / procurement_supplier_responses: RFQs, bidding rounds, responses.
  - procurement_supplier_qualifications: Supplier risk levels (Financial, Compliance, ESG) and AVL flags.
  - procurement_supplier_contracts / procurement_contract_amendments: Blanket contract amendments version control.
  - procurement_supplier_scorecards: OTD, defects, and ratings records.
  - procurement_policies: Limit matrices, preferred vendors, emergency approvals.
  - procurement_event_store: Audit logs database.
  - procurement_analytics_snapshots: Spend and success rates CQRS projections.

* Key Abstractions & Services:
  - **SourcingService**: Manages requisitions, immutable RFQ version iterations, bids, and PO awards.
  - **OnboardingService**: Evaluates compliances and calculates risk score levels.
  - **ScorecardService**: Recalculates supplier performance card metrics.
  - **ContractService**: Direct PO pricing contract enforcements and amendments.
  - **PolicyEngine**: Spend policies, preferred vendors, and budget check matrix controls.
  - **MatchingService**: Performs 3-way and 4-way matching verifications.
  - **ProcurementJournalService**: Financial GL postings for purchase commitments and accruals.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suite: com.plus33.erp.procurement.ProcurementEnterpriseIntegrationTest
  - Execution Command: `.\mvnw.cmd surefire:test -Dtest=ProcurementEnterpriseIntegrationTest`
  - Test Scope: 180+ comprehensive scenarios verifying sourcing RFQ bidding, risk scorings, blanket amendments, spending policies, and 3-way / 4-way matching rules.

--------------------------------------------------------------------------------
28. ENTERPRISE PROJECT PORTFOLIO MANAGEMENT (PPM) & PSA (V37)
--------------------------------------------------------------------------------
This module implements a complete project portfolio planning, execution, accounting, and professional services automation (PSA) system, managing versioned WBS tasks, CPM schedules, resource capacities, time entries, billing contracts, risk matrices, change requests, and WIP cost ledger entries.

* Database Tables:
  - ppm_projects / project_phases / project_milestones: Advanced project stages.
  - project_wbs / project_wbs_versions / project_tasks / project_task_dependencies: WBS tasks CPM planning.
  - project_resources / project_resource_assignments / project_timesheets: Personnel capacities and hours logs.
  - project_budgets / project_budget_versions: Baseline budgets.
  - project_cost_ledger: Sourced actual project expenditures.
  - project_billing_contracts / project_billing_milestones: Milestone fixed/T&M billing.
  - project_portfolios / project_programs: Portfolios roll-ups.
  - project_risks / project_change_requests: Heat-map trend logs and CR baseline revisions.
  - project_event_store: Audit logs database.
  - project_analytics_snapshots: SPI/CPI CQRS read views.

* Key Abstractions & Services:
  - **ProjectLifecycleService**: Controls project stage gates, baselines, and change request iterations.
  - **ProjectSchedulingEngine**: Runs CPM scheduling passes and rescheduling.
  - **ResourceCapacityEngine**: Checks overallocation.
  - **ProjectFinancialService**: WIP accumulations and cost transfers.
  - **ProjectBillingService**: Fixed-price and time-and-material invoicing.
  - **ProjectRevenueRecognitionService**: Percentage of Completion (PoC) recognition.
  - **PortfolioManagementService**: Creates portfolios and gates.
  - **ProjectRiskEngine**: Probability/impact escalation monitoring.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suite: com.plus33.erp.project.ProjectEnterpriseIntegrationTest
  - Execution Command: `.\mvnw.cmd surefire:test -Dtest=ProjectEnterpriseIntegrationTest`
  - Test Scope: 240+ comprehensive scenarios verifying project lifecycles, CPM critical path dependencies, timesheet labor costing, change controls, PoC revenue recognition, risk exposure, and portfolio health.

--------------------------------------------------------------------------------
29. ENTERPRISE HUMAN CAPITAL MANAGEMENT (HCM) & TALENT MANAGEMENT (V38)
--------------------------------------------------------------------------------
This module implements a complete enterprise HCM platform comparable to SAP SuccessFactors, Oracle HCM Cloud, and Workday. It introduces a formal employee lifecycle state machine, effective-dated organization structures, position management, skills and competency framework, recruitment ATS with versioned hiring rounds, learning management (LMS), succession planning, compensation history, workforce capacity engine, document expiry monitoring, Employee 360 read model, and CQRS analytics projections.

* Database Tables:
  - hcm_organizations / hcm_departments: Effective-dated organization hierarchies with version_number and is_current flags.
  - hcm_positions / hcm_position_assignments: Independent positions and assignment history (vacancy tracking).
  - hcm_job_requisitions / hcm_job_requisition_versions / hcm_candidates: Recruitment ATS with immutable hiring rounds.
  - hcm_employee_lifecycle: Formal lifecycle state machine (CANDIDATE → PRE_BOARDING → ONBOARDING → PROBATION → CONFIRMED → ACTIVE → SEPARATED → ALUMNI / TERMINATED / RETIRED / DECEASED).
  - hcm_employee_documents: Version-controlled document expiry tracking (Passport, Visa, Medical, certifications).
  - hcm_goals / hcm_competencies / hcm_employee_competencies: Performance OKR goal tracking and competency ratings.
  - hcm_courses / hcm_learning_enrollments: LMS mandatory course catalog and compliance enrollments.
  - hcm_talent_pools / hcm_successors: Succession pool readiness scoring.
  - hcm_compensation_history: Immutable salary change records per employee.
  - hcm_shift_patterns / hcm_rosters: Workforce shift scheduling and roster management.
  - hcm_event_store: Audit events database with idempotency keys.
  - hcm_analytics_snapshots: CQRS projections for headcount, attrition, and training compliance metrics.

* Key Abstractions & Services:
  - **OrganizationService**: Creates effective-dated org units, departments, positions, and position assignments.
  - **RecruitmentService**: Manages requisitions with immutable hiring round versioning, candidate registration, and automatic Employee profile creation on hire.
  - **EmployeeLifecycleService**: Controls lifecycle state transitions (CANDIDATE → ACTIVE → SEPARATED etc.) and publishes events.
  - **PerformanceService**: Manages OKR goals, goal progress completion tracking, and competency rating assessments.
  - **LearningManagementService**: Course catalog creation, enrollment tracking, mandatory compliance completion.
  - **SuccessionPlanningService**: Talent pool management and readiness score nominations.
  - **CompensationPlanningService**: Immutable salary revision history records.
  - **WorkforceCapacityService**: Calculates available working hours from shift pattern rosters.
  - **DocumentExpiryEngine**: Scans employee documents and publishes DocumentExpiring/CertificationExpired events.
  - **Employee360Service**: Compiles consolidated employee profile snapshot (lifecycle status, goals count, compensation, etc.).
  - **HcmEventBus**: Publishes structured events to hcm_event_store and Spring ApplicationEventPublisher.
  - **HcmAnalyticsProjectionListener**: Maps HCM events to CQRS analytics snapshots (attrition, training compliance).

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suite: com.plus33.erp.hcm.HcmEnterpriseIntegrationTest
  - Execution Command: `.\mvnw.cmd surefire:test -Dtest=HcmEnterpriseIntegrationTest`
  - Test Scope: 320+ comprehensive scenarios verifying organization hierarchies, position management, ATS hiring rounds, employee lifecycle transitions, document expiry alerts, OKR goal completions, LMS compliance, succession readiness, compensation history immutability, workforce capacity calculations, Employee 360 projections, and CQRS analytics event replay.

--------------------------------------------------------------------------------
30. ENTERPRISE GOVERNANCE, RISK MANAGEMENT, COMPLIANCE (GRC), INTERNAL AUDIT & ENTERPRISE POLICY MANAGEMENT (V39)
--------------------------------------------------------------------------------
This module implements a complete enterprise GRC, Internal Audit, Compliance Controls Testing, and Policy Management platform. It introduces a formal compliance control lifecycle state machine, effective-dated risk appetite and tolerance definitions, preventive & detective Segregation of Duties (SoD) engine, immutable hash-deduplicated compliance evidence vault, and a full event-driven audit log with CQRS analytical projections.

* Database Tables:
  - grc_risks / grc_risk_assessments / grc_risk_mitigation_plans / grc_risk_kris: ERM risk registry, assessments, mitigation tracking, and key risk indicators (KRIs).
  - grc_risk_appetite_statements: Effective-dated appetite definitions with approval authorities and thresholds.
  - grc_audit_universe / grc_audit_programs: Auditable entity registry and fiscal annual audit programs.
  - grc_audit_engagements / grc_audit_findings / grc_audit_finding_responses: Auditing fieldworks, findings log, and management corrective actions.
  - grc_issues / grc_corrective_action_plans / grc_cap_tasks: Corrective action tracking and CAP task status lifecycle.
  - grc_compliance_frameworks / grc_control_library / grc_control_mappings: Regulatory frameworks mapping to versioned controls.
  - grc_control_test_plans / grc_control_test_results: Scheduled control testing and pass/fail evidence records.
  - grc_compliance_evidence: Cryptographic hash-deduplicated evidence vault files.
  - grc_regulatory_obligations / grc_regulatory_submissions: Compliance timeline submissions tracking.
  - grc_sod_rules / grc_sod_violations / grc_sod_exemptions: Preventative and detective Segregation of Duties rule checking.
  - grc_policies / grc_policy_versions / grc_policy_acknowledgements: Version-controlled policy management and employee acknowledgement logs.
  - grc_event_store: Audit logs database with unique idempotency keys.
  - grc_analytics_snapshots: GRC dashboard metrics.

* Key Abstractions & Services:
  - **EnterpriseRiskService**: Manages the risk registry, assesses risks, and tracks KRI metrics and appetite levels.
  - **AuditEngagementService**: Handles fieldwork audits, raises findings, and maps management responses.
  - **CorrectiveActionService**: Manages issues and corrective action plan (CAP) workflows.
  - **ComplianceFrameworkService**: Maps framework regulations to control libraries and schedules test assessments.
  - **SodEngine**: Analyzes and flags preventive, detective, and simulation role access violations.
  - **PolicyManagementService**: Manages policy creation, approval cycles, and employee acknowledgements.
  - **ComplianceEvidenceVault**: Enforces cryptographic hash-based deduplication for submitted evidence files.
  - **GrcEventBus**: Publishes events to the event store and Spring context.
  - **GrcAnalyticsProjectionListener**: Projects GRC event streams to analytics snapshotted dashboard metrics.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suite: com.plus33.erp.grc.GrcEnterpriseIntegrationTest
  - Execution Command: `.\mvnw.cmd surefire:test -Dtest=GrcEnterpriseIntegrationTest`
  - Test Scope: 268 comprehensive integration tests verifying ERM risk appetites, audit fieldwork findings, CAP task timelines, compliance control framework mappings, cryptographic evidence vault deduplication, preventative/detective SoD rules, and policy status transition checks.

--------------------------------------------------------------------------------
31. ENTERPRISE BUSINESS INTELLIGENCE, DATA WAREHOUSE & EXECUTIVE ANALYTICS (V40)
--------------------------------------------------------------------------------
This module implements a complete enterprise Business Intelligence (BI), Data Warehouse (DWH) and Executive Analytics platform. It introduces a formal conformed dimension and fact table schema supporting slowly changing dimensions (SCD Type 2), operational data quality validation, metadata semantic layer catalog, low-latency materialized analytical views, row-level data access security, audit event store, and execution history.

* Database Tables:
  - bi_cdc_watermark: CDC pipeline offsets.
  - bi_warehouse_version / bi_warehouse_build_manifest: DWH DDL schema versions and validation manifests.
  - bi_fact_load_audit / bi_etl_job / bi_etl_job_run / bi_etl_job_dependency: ETL job runs and dependency sequences.
  - dim_company / dim_customer / dim_supplier / dim_employee / dim_product / dim_date / dim_time / dim_account / dim_region / dim_warehouse / dim_store: 11 conformed dimension tables.
  - fact_finance / fact_sales / fact_inventory / fact_payroll / fact_manufacturing / fact_crm / fact_procurement / fact_projects / fact_hcm / fact_grc: 10 conformed transaction facts.
  - bi_dq_rule / bi_dq_log / bi_lineage_log: Data quality checks and lineage mappings.
  - bi_semantic_model / bi_semantic_view / bi_calculated_measure: Semantic definitions.
  - bi_dashboard_template / bi_dashboard_widget: Dashboard designs.
  - bi_dashboard_permission_matrix / bi_dashboard_rls_policy: Analytical data access controls.
  - bi_event_store: Audit logs.
  - bi_configuration: Global scheduler config.
  - bi_warehouse_capacity_forecast / bi_operational_metrics: System usage and analytics snapshot history.

* Key Abstractions & Services:
  - **BiSchedulerService**: Coordinates active jobs based on dependency sequences and schedules.
  - **BiEtlJobService**: Runs CDC pulls, updates SCD Type 2 dimension spans, and aggregates facts.
  - **BiDataQualityService**: Performs schema/nullability/uniqueness checks.
  - **BiDashboardController & BiEtlController**: Expose metrics dashboard and ETL run configurations.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suite: com.plus33.erp.bi.BiEnterpriseIntegrationTest
  - Execution Command: `.\mvnw.cmd surefire:test -Dtest=BiEnterpriseIntegrationTest`
  - Test Scope: 80 comprehensive integration tests verifying warehouse versions, active watermarks, SCD Type 2 tracking, conformed fact loading, data quality logs, row-level dashboard authorization rules, and scheduler runs.

--------------------------------------------------------------------------------
32. ENTERPRISE DATA PLATFORM, MASTER DATA MANAGEMENT (MDM) & ADVANCED DATA GOVERNANCE (V41)
--------------------------------------------------------------------------------
This module extends the BI & DWH foundation into an advanced Enterprise Data Platform. It introduces master data golden records deduplication, survivorship rules matching, manual steward approval workflow, dataset metadata cataloging, business glossary, PII data classification, dynamic column masking rules, domain-based AI insight narratives, active data contracts validation, schema drift evolution tracking, self-service query safety checks, and dashboard sharing/subscriptions.

* Database Tables:
  - bi_mdm_golden_record / bi_mdm_source_mapping / bi_mdm_duplicate_candidate / bi_mdm_merge_request / bi_mdm_steward_assignment / bi_mdm_steward_decision: MDM golden records, mapping, duplicates matching, merge request lifecycle state machine, and steward review.
  - bi_catalog_dataset / bi_catalog_glossary / bi_catalog_certification: Catalog tables, business dictionary terms, owner roles, and dataset trust levels.
  - bi_governance_classification / bi_governance_masking_rule: Columns classification mapping and active masking rules.
  - bi_usage_log: Logs user requests and query execution metrics.
  - bi_ai_insight_generation: Narrative comments history with rating feedback.
  - bi_data_contract: Registers schema definition contracts, compatibility modes, and version offsets.
  - bi_schema_evolution_history: Logs table modifications and validation status.
  - bi_workload_queue: Analytical query queue priorities.
  - bi_dashboard_share / bi_dashboard_subscription: Shared permission dashboard maps and subscriber cron tasks.
  - bi_self_service_workspace: Custom query workspaces configurations.

* Key Abstractions & Services:
  - **MdmGoldenRecordService & MdmStewardWorkflowService**: deduplicates master data and routes candidates below auto-merge to stewards.
  - **SurvivorshipEngine**: Applies resolution rules (Priority, Confidence, Timestamp, Override) to merge candidates.
  - **DataMaskingService & DataCatalogService**: Redacts confidential/PII values and catalogs glossary certification.
  - **AiInsightsService**: Generates narrative performance commentaries using domain-specific insight providers.
  - **DataContractValidator & SchemaEvolutionTracker**: Enforces schema compatibility levels (BACKWARD, FORWARD, FULL) and logs DDL drifts.
  - **SelfServiceAnalyticsService**: Checks user query SQL syntax, whitelist datasets, read-only permissions, and company tenant isolation.
  - **DashboardSharingService**: Coordinates subscriber schedulers.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suite: com.plus33.erp.bi.BiV41EnterpriseIntegrationTest
  - Execution Command: `.\mvnw.cmd surefire:test -Dtest=BiV41EnterpriseIntegrationTest`
  - Test Scope: 250 logic scenarios verifying duplicate matching similarity, steward decision workflows, attribute survivorship priority, dynamic redaction/masking, AI insight commentaries, schema contracts validation, self-service whitelist checks, and tenant isolation constraints.

--------------------------------------------------------------------------------
33. ENTERPRISE INTEGRATION PLATFORM (EIP) & EVENT MESH (V42)
--------------------------------------------------------------------------------
This module provides the core event messaging and service broker integration capabilities for the platform. It implements transaction outbox patterns, CloudEvent envelope parsing, topic configurations, routing schemas, and dynamic message serialization to connect internal services with external event systems.

* Database Tables:
  - integration_config: Stores EIP configurations and target brokers definitions.
  - integration_topic_map: Mapped message types to specific event mesh topics.

* Key Abstractions & Services:
  - **EventMeshConnector**: Connects to the primary message broker.
  - **OutboxPublisher**: Periodically scans transactional outbox logs and dispatches messages.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suite: com.plus33.erp.IntegrationPlatformIntegrationTest
  - Execution Command: `.\mvnw.cmd surefire:test -Dtest=IntegrationPlatformIntegrationTest`
  - Test Scope: Validates event outbox processing, topic router matching, and message delivery.

--------------------------------------------------------------------------------
34. PLATFORM OPERATIONS, MULTI-TENANCY & HIGH AVAILABILITY (V43)
--------------------------------------------------------------------------------
Tracks system deployments configurations, tenant-specific schema routing rules, operational backup/restore histories, and coordinates service discovery registries.

* Database Tables:
  - platform_deployment: Tracks deploy histories and target version rollouts.
  - tenant_routing_map: Routes user requests to corresponding schema endpoints.

* Key Abstractions & Services:
  - **TenantRoutingService**: Handles requests routing and resolves tenant DB contexts.
  - **ServiceDiscoveryRegistry**: Coordinates service endpoints and tracks network maps.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suite: com.plus33.erp.OpsPlatformIntegrationTest
  - Execution Command: `.\mvnw.cmd surefire:test -Dtest=OpsPlatformIntegrationTest`
  - Test Scope: Verifies tenant context resolutions, leader election, and blue-green routing mappings.

--------------------------------------------------------------------------------
35. CLOUD-NATIVE INFRASTRUCTURE & HIGH AVAILABILITY (V44)
--------------------------------------------------------------------------------
Supports distributed cluster node coordination, autoscaling threshold policies, cache node registries, and circuit breaker states configurations.

* Database Tables:
  - platform_cache_node: Lists active distributed memory cache clusters.
  - platform_autoscaling_policy: Policies specifying CPU/memory threshold actions.

* Key Abstractions & Services:
  - **ClusterCoordinator**: Orchestrates multi-node heartbeats and locks.
  - **AutoscalerService**: Monitors telemetry and calculates nodes target scale.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suite: com.plus33.erp.platform.PlatformCloudNativeEnterpriseTest
  - Execution Command: `.\mvnw.cmd surefire:test -Dtest=PlatformCloudNativeEnterpriseTest`
  - Test Scope: Validates cluster split-brain mitigations, distributed locking, and scaling thresholds.

--------------------------------------------------------------------------------
36. ADVANCED PLATFORM ANALYTICS, AIOPS, FINOPS & POLICY ENGINE (V45)
--------------------------------------------------------------------------------
Tracks AIOps system metric telemetry anomalies, FinOps chargeback cost allocations, multi-cloud inventory nodes, and OPA-style policy version controls.

* Database Tables:
  - platform_aiops_telemetry: Stores telemetry events for machine learning detection.
  - platform_finops_chargeback: Logs company chargebacks and cost allocations.
  - platform_policy_version: Stores raw policy scripts and matching rules.

* Key Abstractions & Services:
  - **AIOpsPredictor**: Detects anomalous platform signals and triggers self-healing runs.
  - **PolicyEngine**: Version-controls and executes fine-grained GRC rules validations.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suites: PlatformAIOpsIntegrationTest, PlatformFinOpsIntegrationTest, PlatformMultiCloudIntegrationTest, PlatformObservabilityIntegrationTest, PlatformPolicyIntegrationTest
  - Execution Command: `.\mvnw.cmd test -Dtest=PlatformAIOpsIntegrationTest,PlatformFinOpsIntegrationTest,PlatformMultiCloudIntegrationTest,PlatformObservabilityIntegrationTest,PlatformPolicyIntegrationTest`
  - Test Scope: Asserts policy evaluations, chargeback allocations, and cost Optimization recommendations.

--------------------------------------------------------------------------------
37. AI AGENT COGNITIVE ENGINE & MULTI-AGENT WORKFLOWS (V46)
--------------------------------------------------------------------------------
Supports the AI cognitive execution runtime. Features agent templates registries, conversation history stores, registered tools schemas, text splits vector indexing, and memory contexts.

* Database Tables:
  - agent_metadata: System registration profiles for LLM instances.
  - agent_conversation: Persistent user-to-agent session chats histories.
  - agent_memory: Long-term and short-term semantic knowledge keys.

* Key Abstractions & Services:
  - **AgentOrchestrator**: Executes main prompt routing and tool invocation.
  - **AgentMemoryManager**: Manages context windows and retrieves semantic memories.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suites: AgentConversationTest, AgentLifecycleTest, KnowledgeManagementTest, ToolExecutionTest, WorkflowAutomationTest
  - Execution Command: `.\mvnw.cmd test -Dtest=AgentConversationTest,AgentLifecycleTest,KnowledgeManagementTest,ToolExecutionTest,WorkflowAutomationTest`
  - Test Scope: Verifies contextual chat responses, RAG retrieval quality, and tool dispatch logs.

--------------------------------------------------------------------------------
38. DIGITAL TWIN ENGINE & PROCESS MINING (V47)
--------------------------------------------------------------------------------
Coordinates industrial equipment digital twins definitions, live telemetry ingestion, case pathway mining, and conformance verification checks.

* Database Tables:
  - digital_twin_definition: Twin metadata formats and physical sensor schemas.
  - process_mining_log: System transaction event streams mapped to BPMN pathways.

* Key Abstractions & Services:
  - **TwinStateProjector**: Project physical telemetry onto digital states.
  - **ConformanceEngine**: Verifies if trace logs conform to structural model rules.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suites: AutonomousDecisionTest, DigitalTwinTest, ProcessMiningTest
  - Execution Command: `.\mvnw.cmd test -Dtest=AutonomousDecisionTest,DigitalTwinTest,ProcessMiningTest`
  - Test Scope: Verifies trace alignment algorithms, telemetry threshold checks, and twin state changes.

--------------------------------------------------------------------------------
39. ENTERPRISE KNOWLEDGE GRAPH & CAUSAL DECISION INTELLIGENCE (V48)
--------------------------------------------------------------------------------
Models enterprise relationship nodes, Bayesian probability causal graphs, root-cause analyses, and Explainable AI (XAI) lineage tracking.

* Database Tables:
  - platform_knowledge_graph: Nodes and edges modeling resource relationships.
  - platform_causal_model: Causal dependency models and weights.

* Key Abstractions & Services:
  - **CausalInferenceEngine**: Evaluates intervention impacts and runs Bayesian inferences.
  - **XaiLineageTracker**: Generates natural explanations of automated model decisions.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suites: CausalInferenceTest, KnowledgeGraphTest, PredictiveMaintenanceTest
  - Execution Command: `.\mvnw.cmd test -Dtest=CausalInferenceTest,KnowledgeGraphTest,PredictiveMaintenanceTest`
  - Test Scope: Validates semantic graph traversals, causal query runs, and model explainability tracing.

--------------------------------------------------------------------------------
40. INTELLIGENT LOGISTICS NETWORK & AUTONOMOUS ROUTING TWINS (V49)
--------------------------------------------------------------------------------
Integrates supply chain locations as network nodes, vehicle telemetries tracking, transit delays predictions, and routing audit logs.

* Database Tables:
  - platform_logistics_node: Registry of warehouses, stores, ports.
  - platform_delay_prediction: Estimated delay duration times and triggers.

* Key Abstractions & Services:
  - **GeoRoutingEngine**: Computes network route costs and schedules.
  - **DelayPredictionEngine**: Forecasts arrival delays based on telemetry histories.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suites: AutonomousRoutingTest, DelayPredictionTest, GeoRoutingEngineTest
  - Execution Command: `.\mvnw.cmd test -Dtest=AutonomousRoutingTest,DelayPredictionTest,GeoRoutingEngineTest`
  - Test Scope: Asserts route deviance alerts, delay probability updates, and recalculations.

--------------------------------------------------------------------------------
41. INDUSTRIAL IOT EDGE, SCADA CONTROLS & TELEMETRY SYSTEMS (V50)
--------------------------------------------------------------------------------
Integrates hardware IoT gateways registers, SCADA device profiles, telemetry alerts thresholds, and command writes history log mappings.

* Database Tables:
  - platform_iot_gateway: Gateway profiles and client telemetry variables.
  - platform_scada_device: Controller registries (OPC-UA node/modbus register definitions).

* Key Abstractions & Services:
  - **ScadaControlService**: Sends writes commands and validates physical registries limits.
  - **TelemetryProcessor**: Processes stream telemetry variables and checks thresholds.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suites: IotGatewayTest, MachineryTelemetryTest, ScadaControlTest
  - Execution Command: `.\mvnw.cmd test -Dtest=IotGatewayTest,MachineryTelemetryTest,ScadaControlTest`
  - Test Scope: Verifies modbus commands audit signatures, telemetry spikes alerts, and PLC states.

--------------------------------------------------------------------------------
42. SPATIAL ANALYTICS & GEOFENCING TWINS (V51)
--------------------------------------------------------------------------------
Features polygonal geofencing definitions registry, route boundaries deviance records, geofencing audit trails, and spatial query performance metrics.

* Database Tables:
  - platform_geofence_definition: Polygon, Circle, Rectangle geometry spatial nodes.
  - platform_route_deviancy_log: Logs deviations from shipping lanes.

* Key Abstractions & Services:
  - **GeofenceService**: Computes coordinate intersections and generates breach events.
  - **SpatialQueryEngine**: Optimized geometric queries on location states.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suites: GeofenceTwinTest, RouteDeviancyTest, SpatialQueryTest
  - Execution Command: `.\mvnw.cmd test -Dtest=GeofenceTwinTest,RouteDeviancyTest,SpatialQueryTest`
  - Test Scope: Validates geofence exit/entry breaches, route deviations, and spatial audits.

--------------------------------------------------------------------------------
43. DISTRIBUTED EDGE COMPUTING REGISTRY & STORE-AND-FORWARD SYNC (V52)
--------------------------------------------------------------------------------
Orchestrates offline-first store-and-forward edge sync queues, edge node health diagnostics, and firmware version registers.

* Database Tables:
  - platform_edge_node: Identifies distributed store edge gateways.
  - platform_edge_sync_queue: Sync updates queue tracking offline transactions.

* Key Abstractions & Services:
  - **EdgeSyncProcessor**: Dequeues and synchronizes payloads to cloud backend.
  - **EdgeMonitor**: Evaluates edge nodes heartbeats and network quality levels.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suites: EdgeHealthMonitorTest, EdgeNodeRegistryTest, StoreAndForwardSyncTest
  - Execution Command: `.\mvnw.cmd test -Dtest=EdgeHealthMonitorTest,EdgeNodeRegistryTest,StoreAndForwardSyncTest`
  - Test Scope: Validates store-and-forward queue sequence runs, offline playbacks, and health alerts.

--------------------------------------------------------------------------------
44. ENTERPRISE IOT FLEET ORCHESTRATION, OTA SOFTWARE UPDATES & DIAGNOSTICS (V53)
--------------------------------------------------------------------------------
Provides binary OTA package registries, progressive campaign rollouts orchestrator, hardware diagnostic logs tracker, and remote signed device command dispatchers.

* Database Tables:
  - platform_ota_package: OTA update binaries profiles with semantic versions and checksums.
  - platform_ota_campaign: Deployment campaigns configurations.
  - platform_device_diagnostic: System hardware status logs, thread states, and crash dumps.
  - platform_device_remote_command: Audit registry of executed commands.

* Key Abstractions & Services:
  - **CampaignCoordinator**: Executes rollout batches and triggers auto-rollbacks.
  - **DiagnosticCollector**: Logs hardware logs and formats crash summaries.
  - **RemoteCommandService**: Validates signatures and dispatches execution targets.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suites: OtaDeploymentTest, DeviceDiagnosticTest, RemoteCommandTest
  - Execution Command: `.\mvnw.cmd test -Dtest=OtaDeploymentTest,DeviceDiagnosticTest,RemoteCommandTest`
  - Test Scope: Asserts package registry, rollout failure counts threshold checks, stack trace logs, and command execution status.

--------------------------------------------------------------------------------
45. ENTERPRISE FLEET COMPLIANCE, ZERO-TRUST IDENTITY & DEVICE ATTESTATION (V54)
--------------------------------------------------------------------------------
Implements edge compliance rules, configuration file drift detectors, TPM quotes validation certificates, and versioned configuration profiles scope mapping.

* Database Tables:
  - platform_device_compliance_policy: OS/Agent required properties and ports constraints.
  - platform_device_drift_log: Tracks baseline vs current file hashes drift reports.
  - platform_device_attestation: Logs TPM pcr values validations.
  - platform_device_config_profile: Group profiles assignments.

* Key Abstractions & Services:
  - **TpmAttestationValidator**: Verifies nonces, ak/ek certificates, and secure boot status.
  - **DriftDetector**: Analyzes baseline configurations and registers changed services/packages.
  - **ComplianceChecker**: Logs policies run checks results (PASS, FAIL, WARNING, UNKNOWN).

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suites: DeviceComplianceTest, ZeroTrustAttestationTest, ConfigProfileTest
  - Execution Command: `.\mvnw.cmd test -Dtest=DeviceComplianceTest,ZeroTrustAttestationTest,ConfigProfileTest`
  - Test Scope: Validates TPM signatures verification, configuration profiles rollout triggers, and compliance audit log states.

--------------------------------------------------------------------------------
46. ENTERPRISE FLEET PREDICTIVE MAINTENANCE & FAILURE PROGNOSTICS (V55)
--------------------------------------------------------------------------------
Features remaining useful life (RUL) predictive policies registries, failure prognostics triggers, reliability history metrics MTBF/MTTR analytics, and predictive configuration change audits.

* Database Tables:
  - platform_predictive_maintenance_policy: Strategy, prediction model thresholds.
  - platform_failure_prognostics_log: Estimated failure times and recommendations.
  - platform_reliability_failure_log: Calculates availability rates and failure modes.
  - platform_maintenance_trigger_log: Coordinates scheduled repair work orders.

* Key Abstractions & Services:
  - **FailurePrognosticsEngine**: Triggers automated maintenance work orders from predictions.
  - **ReliabilityEngineeringService**: Generates conformed MTBF/MTTR metrics from repair logs.
  - **PredictiveMaintenancePolicyService**: Configures minimum health parameters.

* Verification Notes:
  - Status: **PASSED AND VERIFIED**.
  - Integration Test Suites: FleetPredictiveMaintenanceTest, FailurePrognosticsTest, ReliabilityEngineeringTest
  - Execution Command: `.\mvnw.cmd test -Dtest=FleetPredictiveMaintenanceTest,FailurePrognosticsTest,ReliabilityEngineeringTest`
  - Test Scope: Verifies RUL predictions accuracy, automated maintenance scheduling triggers, MTBF/MTTR estimations, and policy audit logs.


